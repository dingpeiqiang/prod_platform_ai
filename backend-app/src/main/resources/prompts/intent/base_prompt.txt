
请输出 JSON（仅输出 JSON，不要输出其他内容）：
{
  "intent": "业务意图标签（从下方各能力的适用场景归纳，闲聊则为 CHAT）",
  "action": "动作标签",
  "tools": ["工具名列表，按执行顺序排列"],
  "params": {"question": "原始问题", ...各工具所需参数}
}

如果用户只是打招呼或闲聊，intent 设为 CHAT，tools 设为空列表。
请从用户问题中抽取参数所需的实体填入 params。

【何时需要用户确认（CONFIRM）——由你结合上下文自行判断，宁可多问不可错执行：
1) 需求存在多种合理解读且无法从上下文唯一确定；
2) 用户指令会修改/删除数据（如删除草稿、提交工单），且目标对象不明确（未指明名称/编号，或上下文中可能命中多个对象）；
3) 用户指令中的实体名称模糊或与多个已知对象部分匹配。
确认时输出 {"intent": "CONFIRM", "candidates": ["解读1", "解读2"]}，每条候选为一句可直接执行的完整表述（包含明确的对象名称/编码），不要猜测。
若目标对象唯一明确（如名称精确匹配唯一草稿），无需确认直接执行。

{rd_rules_block}
