# 手册（Playbook）：完成一类事情的标准作业程序
#
# 产品运营助手·异动归因入口的手册化（原单本 ops-analysis 按前端四入口拆分之一）：
#   异动归因 = 指标查询定位异动 + SWRL 归因推理 + 本体规则解释（为什么动、动在哪、依据什么），
#   本手册只承载归因问诊链路，不掺全量风险稽核（那是 risk-audit 手册的事）。
#   手册是业务流程文档：步骤 = 业务动作（查异动 → 归因 → 解释），tool 是动作的实现调用——
#   业务人员读 do/how 就能照着办事，工具名只是技术注脚。
#   ① 路由消费：applies_to.intents 命中 → SceneFlowRouter 回落动态编排，LLM 照手册执行
#   ② 编排消费：renderSop 注入 system prompt（DefaultUnderstander.playbookSopSection），
#      intent_guide 随 SOP 下发——LLM 依据手册归口声明选意图码（手册 = 意图定义源）
#   手册直达链路（sop-step-N 时间线 + 环节 IO 差异化）：理解层 LLM 照 intent_guide 归口输出意图码命中 applies_to.intents →
#   AgentOrchestrator 升级 runPlaybookPath/runPlaybookStream（归一化映射仅兜底自由意图）
#
# 命名规范：文件名 = 手册 code（kebab-case），与 name 字段一致
# 注意：多工具手册——步骤 tool 必须与真实执行一致，否则该步骤永远等不到执行事件；
#   本手册三步各对应一个真实工具执行（非单工具内部环节切分），依次各执行一次。

# ── ① 元数据 ──────────────────────────────────────────
name: root-cause
title: 异动归因
description: 产品运营助手·异动归因——指标查询定位异动，SWRL 归因推理给出根因路径，本体规则解释收尾给处置依据
version: 1

# 适用域：路由器据此判定"本次请求是否适用本手册"（显式声明，替代场景级一刀切）
# 手册 = 业务流程，路由只认业务意图（intent + 归口话术）——工具是步骤实现细节，
# 不参与业务分流（编排层的工具链从步骤序列自动提取）
applies_to:
  scene: ops
  # ops 场景规范意图码（IntentRecognitionSupport 归一化产物），首项为手册直达链路的计划意图
  intents: [PRODUCT_OPS_REASON]
  # 意图归口（手册 = 意图定义源，LLM 据此选意图码而非自由发挥）：
  #   随 renderSop 注入 system prompt，LLM 依据话术语义匹配归口声明输出 intent——
  #   本手册归口归因问诊（为什么下滑/异动原因/根因分析），
  #   事实查询归 market-insight、规则判定归 risk-audit/online-check（见各自手册声明）
  intent_guide:
    PRODUCT_OPS_REASON: "为什么下滑/异动原因/根因分析等归因问诊"

# ── ② 步骤（操作步骤 + 每步的操作方法和使用的工具）─────
# 手册 = 业务流程（异动归因怎么干），步骤 tool 是「本流程此动作调用该工具」的调用语句
# （工具配置独一份在 AgentTool 注册表，手册只调用不定义）；换工具实现改注册表，手册零改动
steps:
  - id: facts
    do: 查询异动指标事实
    how: 自然语言转 SPARQL 在本体知识库执行语义查询，定位异动商品与关联指标；实体发现 LLM 优先、关键词兜底
    tool: sparql_query

  - id: root-cause
    do: 异动根因归因
    how: SWRL 归因推理（R-A 系列规则）；显式商品优先，缺省从问题语义解析分析对象；归因路径逐条透出
    tool: swrl_root_cause
    policy: 归因结论须可追溯——命中的归因规则与推理引擎（openllet-swrl/java-rules）随 tool 事件下发，不臆造原因

  - id: explain
    do: 本体规则解释收尾
    how: 对归因涉及的本体概念/规则给出自然语言解释，附引用规则编号，让归因结论有据可查
    tool: ontology_explain

# ── ③ 护栏（控制语义：人工门 / 审计 / 兜底）─────────────
guardrails:
  human_gate: []          # 纯分析链路（查询/归因/解释均只读），无写操作，无人工门
  audit: 推理引擎标识（reasonEngine）与命中归因规则编号随 tool 事件下发；分析对象商品（offeringId/Name）随结论透出
  fallback:
    - 事实查询为空 → 归因照常执行（独立数据源），汇总时明确说明事实数据缺失
    - 归因推理无命中路径 → 如实告知未命中归因规则，不编造原因
    - 推理引擎异常 → 明确报错原因，不返回空结论冒充无异常
