# AGENTS.md - 编码与设计质量规范

## 编码质量规范

### 1.去旧留新原则
- **新增功能优先**：开发新功能时，优先使用项目中已有的最新技术栈和架构模式。
- **旧代码清理**：修改或重构旧代码时，同步清理不再使用的废弃代码、注释和无用文件。
- **向后兼容**：在移除旧功能前，需确保所有依赖模块已迁移至新实现。
- **版本标记**：对即将废弃的接口或模块添加 `@deprecated` 注释，并指明替代方案。
- **定期清理**：每个迭代周期结束前，进行一次代码清理。

### 2. 代码风格

**Python**：遵循 PEP 8，4空格缩进，行长≤120字符

**JavaScript/TypeScript**：遵循 Airbnb 规范，2空格缩进，语句末尾加分号

### 3. 命名规范

| 类型 | Python | JavaScript/TypeScript |
| ---- | ------ | --------------------- |
| 变量/函数 | snake_case | camelCase |
| 类/组件 | PascalCase | PascalCase |
| 常量 | UPPER_SNAKE_CASE | UPPER_SNAKE_CASE |
| 文件/目录 | snake_case | kebab-case |

**JSON数据传输统一使用 snake_case**

### 4. 代码审查要点

- PR单一职责，新增代码测试覆盖率≥80%
- 检查正确性、可读性、可维护性、安全性、性能、测试完整性

### 5. 复杂度控制

- 圈复杂度≤10
- 函数长度≤50行
- 类方法数≤20
- 嵌套深度≤4

### 6. 静态分析工具

flake8 + mypy（Python）；eslint + prettier（JS/TS），必须集成CI和pre-commit钩子

---

## 设计模式规范

### 创建型模式
- **工厂模式**：对象创建逻辑复杂时使用
- **单例模式**：全局唯一资源/配置管理器

### 结构型模式
- **策略模式**：算法需灵活切换时使用
- **适配器模式**：接口不兼容时进行转换
- **装饰器模式**：动态添加功能不修改原有代码

### 行为型模式
- **观察者模式**：状态变化需通知多个观察者
- **责任链模式**：请求需多处理器依次处理
- **命令模式**：支持撤销/重做操作

### 架构模式
- **MVC**：分离数据模型、视图展示、请求控制
- **依赖注入**：解耦依赖关系，便于测试

---

## 编码原则

### SOLID原则
- **SRP**：单一职责
- **OCP**：对扩展开放、对修改关闭
- **LSP**：子类可替换父类
- **ISP**：接口隔离
- **DIP**：依赖倒置

### 核心原则
- **DRY**：避免重复代码
- **KISS**：保持简单易懂
- **YAGNI**：不实现当前不需要的功能
- **高内聚低耦合**：模块内高度相关，模块间松散耦合
- **关注点分离**：数据访问、业务逻辑、API控制、验证、配置分离

### 代码可读性
- 使用有意义的命名
- 适当注释解释"为什么"而非"做什么"
- 避免魔法数字，使用常量替代

---

**最后更新**：2026-06-03  
**版本**：v2.1