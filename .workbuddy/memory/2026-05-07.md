# 2026-05-07

## 前端聊天逻辑重构 - useChatCore 提取

### 问题分析
- DashboardHome 组件未被 App.vue 集成（grep 无引用），是死代码
- 首页聊天窗口实际上是 ChatAssistant 的 welcome-screen（）
- ChatAssistant 的 sendMessage/handleEvent/enqueueSave/ensureDbSession 与 DashboardHome 的 emit('send-message') 完全独立

### 重构方案
创建 （453行）：
-  - 完整发送+SSE流式处理
-  - SSE事件处理
-  - 会话创建管理
-  - 消息保存队列
-  - 停止生成
- 通过 callbacks（onScrollToBottom, onResetInput, onSessionInit, onTitleUpdate）解耦DOM操作

### ChatAssistant.vue 改动
- 移除 sendMessage（196行）、handleEvent（133行）、ensureDbSession、enqueueSave/processSaveQueue 等
- 移除 module-level abortCtrl/sessionCreating/saveQueue 变量
- 新增 useChatCore 初始化（8行），sendMessage/handleEvent 包装器（各2行）
- 保留：UI逻辑、表单处理（generateForm/updateFormFields）、配置处理、Intent事件注册
- requestValidation 函数有独立 local abortCtrl（不受影响）

### DashboardHome.vue 改动
- 添加 messages-area（v-if="messages.length > 0"）
- 集成 useChatCore，handleSend/handleSuggestion 调用 coreSendMessage
- 移除 emit('send-message')（不再依赖父组件）
- 独立 session 管理（localStorage keys: dashboard_local_session_id）
- 新增消息展示样式（bubble/ai-text/cursor-blink/dots-loading）

### 关键设计
- useChatCore 不处理 DOM（scrollToBottom 通过 callback）
- inputText 由调用方通过 onResetInput callback 管理
- genId/formatMarkdownText 同时存在于 useChatCore 和 ChatAssistant（功能相同，作用域不同）


# 2026-05-07 工作记录

## 处理步骤规范整改

### 背景
用户要求评估项目中处理步骤是否满足规范，发现各 Handler 注释风格不统一。

### 完成工作

1. **整改所有 Handler 注释风格**，统一为：
   - `# ═══ Phase X：标题 ═══` — Phase 标注
   - `# ── Step X：子步骤` — Step 子步骤

2. **整改 Handler 列表**：
   - FormHandler ✅
   - FormUpdateHandler ✅（添加 Phase 标注）
   - DeleteFormHandler ✅
   - ManageHistoryHandler ✅
   - ConfigureHandler ✅
   - ChatHandler ✅（添加 Phase 标注）
   - ValidationHandler ✅（补充 stats 输出 + Phase 标注）
   - TariffFilingHandler ✅

3. **更新 AGENTS.md 开发规范**，新增章节：
   - "意图处理器处理步骤规范"
   - Phase 模板结构
   - Phase 定义表格
   - 注释风格规范
   - SSE 事件类型表
   - thinking 消息格式
   - 新增意图类型流程

### 规范要点

Phase 模板：
- Phase 1：识别 (Identify) — 分析输入，确定任务
- Phase 2：执行 (Execute) — 核心业务逻辑
- Phase 3：输出 (Output) — SSE 事件输出

### 相关文件
- `backend/app/intent/handlers/*.py` — 8个 Handler 全部整改
- `AGENTS.md` — 新增处理步骤规范章节

## 历史填写记录功能

### 完成工作
- 新增"填写表单"跳转按钮（顶部栏蓝色按钮，生成表单后显示，点击展开+滚动到右侧表单）
- 实现历史填写记录功能（前端本地存储 + 新后端API）：
  - `backend/app/api/form.py`：新增 `GET /form/history/{form_code}/submissions` 和 `GET /form/instance/{form_id}` 端点
  - `frontend/src/components/ChatAssistant.vue`：`submissionHistory` ref、localStorage 存储/加载、`saveSubmissionRecord`（提交时保存）、`removeSubmissionRecord`、`replaySubmission`（复用历史数据填写）、`handleReplaySubmission`/`handleRemoveSubmission` 事件处理
  - `frontend/src/components/intent-panels/SubmissionHistoryPanel.vue`（新建）：显示历史记录列表、展开详情、复用/删除按钮
  - 集成到消息气泡渲染：`v-if="msg._submissionHistory?.length"` 条件渲染
  - `clearHistory` 时同步清空历史记录

## 聊天记录数据库持久化
- 前端新建 `src/services/chatApi.js`：封装 createSession/saveMessage/loadMessages/getSessions/deleteSession/updateSessionTitle
- 后端新增 `POST /api/v1/chat/sessions/{session_id}/messages` 端点，保存单条消息到 chat_messages 表
- ChatAssistant.vue：叠加 DB 持久化
  - 发送消息后 `await saveMessage()` 落库
  - AI 回复结束后 `saveMessage()` 落库
  - mount 时 `apiLoadMessages()` 从库加载
  - `watch(dbSessionId)` 监听会话切换
  - 首次发消息时 `ensureDbSession()` 创建 DB 会话
- App.vue：管理数据库会话生命周期
  - 登录后 `initDbSessions()` 同步本地会话与 DB
  - 通过 `@session-init` 回调回填本地会话的 `dbSessionId`
  - 删除会话时 `apiDeleteSession()` 同步删除 DB 记录
- 会话数据结构：每条本地会话增加 `dbSessionId` 字段映射到数据库

## 修复：历史会话加载逻辑（第二次）
- ChatAssistant `watch(() => props.sessionId)` 切换会话时增加 DB 加载分支：有 `currentDbSessionId` 则 `apiLoadMessages()`，无则降级 localStorage（之前只走 localStorage）
- ChatAssistant `onMounted` 移除错误的 `dbMsgs.length === 0 → loadMessages()` 回退：有 `dbSessionId` 时永远以 DB 为准，DB 空则显示空会话（旧版无 `dbSessionId` 的会话才走 localStorage）
- 清理无用的 `dbSessionLinked` 状态变量

## 2026-05-07

### 聊天架构 v2 迁移

**执行内容：**
- 创建 v2 表： /  / 
- 删除 v1 表： / 
- 删除 v1 Service：（业务逻辑已由 v2 ChatServiceV2 覆盖）
- chat.py（业务端点）：恢复 git 备份，移除历史 CRUD 端点，保留所有业务端点
  - 保留端点：/chat/stream, /chat/agent, /chat/agent/stream, /chat/deploy-config, /chat/delete-form, /chat/form-versions, /chat/rollback-form, /chat/history/*
- chatApi.js：重写为 v2，所有业务字段存入 metadata
- v2 CRUD 路由： 系列由 chat_v2.py 提供
- v1 业务路由： 由恢复后的 chat.py 提供（无 history CRUD）
- chat_with_tools.py：保留，独立于 v1/v2

**当前架构：**
- 业务端点：chat.py → /chat/* (无 prefix, root 注册)
- v2 CRUD：chat_v2.py → /api/v2/chat/*
- 业务扩展：全部存入 chat_message_metadata.key/value

**注意事项：**
- v1 历史 CRUD 端点已由 v2 接管，前端 chatApi.js 全部改用 /api/v2/chat
- 业务端点（stream/deploy-config 等）走 /api/v1/chat/*，由 chat.py 提供
- chat_with_tools.py 走 /api/v1/chat_with_tools，与 chat.py 完全不冲突

## 修复：meta_key bug + 错误处理

### 问题
- `ChatServiceV2.save_message` 中 metadata 写入使用 `key=key` 而非 `meta_key=key`，导致 metadata 写入失败
- `chat_v2.py` 中 `save_message` 失败时返回 `200 {success:false}` 而非 HTTP 500

### 修复清单
| 文件 | 修改 |
|------|------|
| `backend/app/services/chat_service_v2.py` | 第200行 `key=key` → `meta_key=key` ✅ |
| `backend/app/api/chat_v2.py` | 第195行 `return {"success": False}` → `raise HTTPException(500)` ✅ |

### 测试验证（2026-05-07 09:12）
- ✅ 创建会话：POST /api/v1/chat/sessions → 200
- ✅ 保存消息（含 metadata）：POST .../messages → 200
- ✅ 查询消息返回正确 metadata：GET .../messages → metadata 字段正确
- ✅ 后端端口：6173（非 8000）

## prod_platform_ai 容器化部署

### 生成文件（2026-05-07）
- `backend/Dockerfile` — Python 3.10-slim 多阶段构建，uvicorn 启动
- `frontend/Dockerfile` — Node 20 构建 + nginx:alpine 运行
- `frontend/nginx.conf` — SPA 路由 + API/WebSocket 代理到后端
- `docker-compose.yml` — 后端(6173) + 前端(80) 编排，含 healthcheck
- `.env.example` — 环境变量模板
- `deploy-docker.bat` — Windows 一键部署脚本
- `deploy-remote.sh` — 远程服务器部署脚本

### 架构说明
- 前端 nginx 反向代理 `/api/` 和 `/ws/` 到后端，解决跨域
- 后端 healthcheck 通过 `/health` 端点检测
- volumes 挂载 `config/` 和数据库目录，保留数据
- `profiles: [production]` 前端默认不启动，生产环境单独启
