# 2026-05-23

- 工作流编辑器"工具调用"节点 → MCP工具节点改造：
  - `workflow_executor.py`：`ToolNodeExecutor._generic_tool_call()` 接入 `MCPToolHub.execute_sync()`，支持真实 MCP 工具执行
  - `ToolNode.vue`：完全重写，从 `GET /api/v1/mcp-management/tools` 动态加载工具，按 category 分组，根据 input_schema 渲染参数
  - `NodePanel.vue`：节点名称改为"MCP工具"，图标 🔌，更新帮助文档
  - 保持 `tool` 类型不变（向后兼容现有工作流 JSON）

- MCP 工具管理优化——出入参 Schema 可视化查看与编辑：
  - 新增 `SchemaViewer.vue`：通用 Schema 展示/编辑组件，只读模式（参数名/类型/必填/描述表格）+ 编辑模式（内联编辑+新增/删除+JSON切换）
  - `tool_def.py`：`MCPTool` dataclass 增加 `output_schema` 字段，`to_mcp_dict()` 输出 `outputSchema`
  - `tool_hub.py`：`register()` / `mcptool` 装饰器增加 `output_schema` 参数
  - `ToolList.vue`：操作列新增"详情"按钮 → el-drawer 展示工具基本信息 + inputSchema + outputSchema（SchemaViewer 只读）
  - `ToolTester.vue`：schema-preview `<pre>` 替换为 SchemaViewer（只读），同时展示 inputSchema 和 outputSchema
  - `ExternalToolManager.vue`：Input/Output Schema textarea 替换为 SchemaViewer（编辑模式），保留 JSON 切换按钮

- MCP 工具分类修正——tariff 工具从内部迁移为外部：
  - `tariff_tools.py`：移除 `@mcptool()` 装饰器（`query_tariff_by_code` / `query_tariff_info`），保留函数本体（tariff_agent/tariff_filing 直接调用不受影响）
  - `__init__.py`：从 `register_all_tools()` 移除 `tariff_tools` 导入，新增 `register_external_tariff_tools()` 以 `category="external"` 注册
  - `main.py`：启动流程调用 `register_external_tariff_tools()`，计入外部工具计数
  - `mcp_management.py`：`_get_category_display_name` 增加 `"external": "外部API工具"`
  - 前端 CallStats.vue / ToolTester.vue：categoryNames 增加 `external: '外部API工具'`

- 删除废弃的 tariff 基础设施：
  - 删除 `app/langchain/tariff_agent.py`（TariffProcessor）
  - 删除 `app/api/tariff_filing.py`（资费备案 API 路由）
  - 清理 `app/langchain/__init__.py` 移除 tariff_agent/tariff_actions 导入
  - 清理 `app/main.py` 移除 tariff_filing_router 注册
  - 清理 `app/engine/workflow_executor.py` _validate_form 移除 TariffProcessor 调用，直接用 _basic_validate
  - 保留：tariff_tools.py（外部 MCP 工具函数）、tariff_actions.py（工作流动作）、workflow_init.py（动作注册）