# 2026-05-11

## sitech-master 分支合并修复

### 问题1：FormService.generate_form 方法缺失
- **原因**：cherry-pick sitech-master 的 5月10日提交时，`FormService` 被 sitech-master 的简化版本覆盖，丢失了 `generate_form` 方法
- **修复**：从 `backup-before-sitech-merge` 分支恢复完整的 `form_service.py`，包含：
  - `generate_form()` - 表单生成主方法
  - `adapt_for_chat_window()` - 适配聊天窗口
  - 其他 CRUD 方法

### 问题2：RecommendationMerger.merge_recommendations 格式处理错误
- **原因**：`HistoryService.get_recommend_values` 返回的是字典列表 `[{value, label, score, source}]`，但 merger期望的是字符串列表
- **修复**：`merger.py` 第 71-87 行，修复 history 推荐的处理逻辑，正确提取 `value`、`label`、`source`

### 推荐策略工作正常
测试结果：
- `llm_rule`: LLM 提取推荐（最高优先级）
- `history`: 历史推荐（需数据库有历史数据）
- `static`: 静态配置兜底

### 备份分支
- `backup-before-sitech-merge`: 合并前的完整备份

## 修复"没有看到模型思考"问题

### 根因
1. `chat_service.py`：流式聊天中 `[THINKING]...[/THINKING]` 被直接丢弃，未转为 reasoning 事件
2. `chat.py`：reasoning 仅限 `form/form_update/configure` 意图发送
3. `useChatStream.js`：流结束后面板自动折叠

### 修复
1. `chat_service.py`：thinking 块 → `yield reasoning()` 事件
2. `chat.py`：移除意图类型限制，所有意图发送 reasoning
3. `useChatStream.js`：有 reasoning 内容时保持面板展开
