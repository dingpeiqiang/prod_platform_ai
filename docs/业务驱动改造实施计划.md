# 《业务驱动改造方案》实施计划明细 v1.0

| 项 | 内容 |
|---|---|
| **日期** | 2026-08-31 |
| **性质** | 实施计划明细（对《业务驱动改造方案.md》v4.4 的落地细化，行号以 2026-08-31 代码基线为准） |
| **配套** | `docs/业务驱动改造方案.md`（设计依据）· 本文档（执行依据） |

---

## 0. 现状代码基线（实施前已核实）

| 文件 | 行数 | 与方案相关的关键位置 |
|---|---|---|
| `frontend/src/utils/normalizeThinkingStep.js` | 232 | LEGACY_STEP_TEMPLATES:9-13；ID_TITLES:15-47；ID_CATEGORY:53-75；pickResultFromContent:96-105；hasBackendTitle 优先逻辑:139-142 |
| `frontend/src/utils/thinkingSchedule.js` | 366 | 9 场景静态剧本（批次③对象） |
| `frontend/src/utils/simulateReply.js` | 149 | createStreamingPlaceholder / sleep（批次③对象） |
| `frontend/src/components/RdAssistantPage.vue` | 1192 | 演示引用：import:89-96；resolveProductScenario:575-599；playProductReply:683-694；normalizePlaybookSteps:697-750；playProductReply 调用点：330/351/437/450/966/1028/1063/1115/1151（共 9 处，playbook 引用 45 处） |
| `backend-app/.../agent/impl/DefaultUnderstander.java` | 561 | 白名单：KNOWN_TOOLS:234-240、RD_KNOWN_TOOLS:243-249；旧枚举兼容:297-314；parseMultiIntents:332-357；mapIntentToPlan:384-406；mapRdIntentToPlan:412-433；sanitizeTools:441-453；buildOpsSystemPrompt:490-512；buildRdSystemPrompt:515-537 |
| `backend-app/.../agent/impl/DefaultPresenter.java` | 212 | CLARIFY_LABELS:32-37；buildClarifyMessage:101-116；suggestFollowUps:119-157（写死工具名分支） |
| `backend-app/.../agent/AgentOrchestrator.java` | 774（实际）/ 方案标注 926 事件行区间 | buildToolEvent:630-655（ThinkingCopy 查表 + label 兜底）；toolInputView:663-691；actionDisplay:743-753 |

> 注：方案正文引用的行号（如 `:393-433`）与当前基线略有偏移，本文档行号为实施时实际核对值，以本文档为准。

---

## 阶段〇：前置确认（0.5 天，阻塞全部阶段）

| # | 任务 | 产出 | 验收 |
|---|---|---|---|
| 0.1 | 评审确认方案 3.3 编排必要性对照表 | 评审签字记录 | 5 类意图编排链路逐条确认 |
| 0.2 | 批次③决策收集（A 演示走真实 SSE / B 懒加载隔离 / C 删除入口） | 产品决策记录 | 未决策则阶段四挂起，不阻塞 M1~M3 |
| 0.3 | 库内无 title 的 `reasoning_full` 老数据命中量统计 | SQL 统计结果 | 命中=0 → 批次②立即执行；>0 → 批次②延后保留 @deprecated |
| 0.4 | 11.4 五项体验指标基线记录 | 基线数据表 | 追问率/一次办结率/跟进转化率/任务链完成度/人工接手成功率（后两者需前端埋点，见任务 3.3） |

---

## 阶段一：前端清理线（M1，预估 2 天）

### 任务 1.1 批次①：删猜测式兜底（normalizeThinkingStep.js）

**前置确认**（三条均已核实成立，实施时复核一次即可）：
1. 后端 thinking 事件全量带 title（`AgentOrchestrator.thinkingStep` 固定带 title/id/content）
2. tool 事件 done 态带 title（`buildToolEvent :636-646`：ThinkingCopy 查表或工具 label 兜底）
3. 前端 hasBackendTitle 优先逻辑存在（`normalizeThinkingStep.js:139-142`）

**删改点明细**：

| 位置 | 现状 | 动作 |
|---|---|---|
| `ID_TITLES`（:15-47，33 条目） | 按 id 猜标题 | **删表 + 删 :142 的 `ID_TITLES[id]` 查表引用**，收敛为：`type === 'ontology' ? '分析推理' : type === 'tool' ? '业务处理' : '处理步骤'` |
| `pickResultFromContent`（:96-105） | 3 条正则从文案反猜 result | **删函数 + 删 :119-125 的调用**，result 收敛为 `raw.result` 或 `meta.intentLabel`，不再从 content 正则猜测 |
| `LEGACY_STEP_TEMPLATES`（:9-13） | 旧格式 3 步模板 | **本批次先加 `@deprecated` 注释**（“批次②清理对象，命中量为 0 后删除”），删改动作在任务 1.2 |

**保留不动**（明确防误删）：
- `ID_CATEGORY`（:53-75）与 `resolveCategory`（:86-94）——分类徽标推断，属 P0 交付的枚举映射，非猜测式兜底
- `CATEGORY_LABELS`（:77-83）——分类枚举显示名（5 个固定值）
- `attachRootCauseOntology`（:216-232）——ontology 可视化挂载，真实链路数据

**预估净删**：约 -60 行。
**验证**：`npm run build` + 研发 5 场景 + 运营 5 场景 + 历史还原手测。
**红线路径**：回归中出现“处理步骤”通用词 → 判定后端漏发 title → 修后端（`AgentOrchestrator`），**禁止**回补前端词典。

### 任务 1.2 批次②：LEGACY_STEP_TEMPLATES 清理（条件执行）

| 前置（任务 0.3 结果） | 动作 |
|---|---|
| 命中量 = 0 | 删 `LEGACY_STEP_TEMPLATES`（:9-13）+ 清理全部引用点：:117 `legacyTpl` 变量、:127 `legacyTpl?.id`、:142 `legacyTpl?.title`、:145 `legacyTpl?.content`，约 -30 行 |
| 命中量 > 0 | 保留原状，仅保留任务 1.1 已加的 @deprecated 注释；禁止新增条目；数据迁移完成后再执行本任务 |

**验证**：历史会话切换还原正常（无 title 老数据渲染通用词而非报错）。

---

## 阶段二：后端低风险改造（M2，预估 3 天）

### 任务 2.1 prompt 动态生成（DefaultUnderstander.java）

| 项 | 内容 |
|---|---|
| 删除 | `buildOpsSystemPrompt()`（:490-512）、`buildRdSystemPrompt()`（:515-537）两份手写字符串；`buildSystemPrompt(boolean)` 的 rd 分支（:485-487）改为单实现 |
| 新增 | `buildSystemPrompt(boolean rdScene)`：遍历场景白名单过滤后的注册表（ops 用 KNOWN_TOOLS / rd 用 RD_KNOWN_TOOLS 既有常量），按 `getName() + getDescription() + describeParams(getParams())` 拼接；输出 JSON 约定段（`{intent?, tools, params}`）保留 |
| 新增 | `describeParams(List<ToolParam>)`：遍历参数契约拼 `名称(说明, 必填)`，≤30 行 |
| 保留 | KNOWN_TOOLS / RD_KNOWN_TOOLS 白名单常量（安全底线，非 prompt 冗余） |
| 风险点 | **过渡期兼容**：现有 prompt 中“意图与工具对应关系”段删除后，LLM 对 product_ops_* 枚举的输出习惯可能变化；`parseLlmResults` 的旧枚举兼容（:297-314）在阶段三前保留不动，作为输出解析侧的缓冲 |
| 验证 | `mvn -q compile` + 改造前后同一批 20 条话术（研发 10 + 运营 10）的 LLM 输出对比：选工具一致率 100%、params 抽取一致率 ≥95%；`understand-diagnosis.log` 无新增不可解析记录 |

### 任务 2.2 澄清文案 LLM 化（DefaultPresenter.java）

| 项 | 内容 |
|---|---|
| 删除 | `CLARIFY_LABELS`（:32-37，4 条模板）|
| 改造 | `buildClarifyMessage`（:101-116）：优先 LLM 基于 `ToolParam.getName()/getDescription()` 生成追问文案；LLM 失败/为空回退一句话模板 `"请补充：{参数业务名}"`（业务名暂用参数名，保持 requireNonBlank 语义） |
| 保留 | :104 的空列表兜底分支（clarify 为空时的固定提示）——它是 CLARIFY 无参数时的合法输出，非猜测式兜底 |
| 契约 | 追问文案仍走 CLARIFY 结构化通道（前端 `ChatMessageList.vue` 补参区），**只改文案来源，不改数据结构** |
| 验证 | 回归清单第 3 项（CLARIFY 澄清流）；文案含参数业务说明且非 4 模板原文 |

### 任务 2.3 未登记工具文案 LLM 化 + 缓存（AgentOrchestrator.java，最小改动）

| 项 | 内容 |
|---|---|
| 改造点 | `buildToolEvent`（:641-645 的 else 分支）：`toolCopy()==null` 且工具无 label 时，调用 LLM 按 ToolCopy 四要素（title/goal/manualHint/category）同构生成一次 |
| 缓存 | `ConcurrentHashMap<String, ThinkingCopy.ToolCopy>` 进程内缓存，key=toolName；生成失败回退现状（label 或无 title） |
| 边界 | `ThinkingCopy.java` 本体零改动（方案 3.1 明确）；不新增字段；生成结果不回写词典文件 |
| 验证 | 临时注册一个无 ThinkingCopy 条目的测试工具 → tool 事件四要素齐全；二次执行命中缓存（日志确认） |

**阶段二退出**：全场景回归 5 项通过；单轮 LLM 调用次数确认 ≤3。

---

## 阶段三：映射退役 + 编排深化（M3，中风险，预估 4 天）

### 任务 3.1 意图映射退役（DefaultUnderstander.java）

| 项 | 内容 |
|---|---|
| 删除 | `mapIntentToPlan`（:384-406）、`mapRdIntentToPlan`（:412-433）两个 switch |
| 改造 | `parseLlmResults` / `parseMultiIntents` 中直接构造：`new QueryPlan(planLabel(firstTool), sanitizeTools(tools, List.of(), rdScene), params, question)`；`intent` 弱化为展示标签（`params.put("intent_type", normalized)` 保留，AgentOrchestrator 的 intentData 还原依赖它） |
| 保留 | ① `sanitizeTools` 白名单守门（:446-453）② `parseMultiIntents` 混合意图拆分（:332-357）③ 旧枚举兼容段（:297-314，SPARQL_QUERY 等映射到业务意图的归一化）——解析的是 LLM 已输出结构，非映射表 ④ `diagnose`/`writeDiagnosis` 诊断（解析失败仍需落盘） |
| 新增守门 | 未注册工具剔除后：若过滤结果为空且 recommended 为空 → 转入“一次重选”（见 3.2） |
| 验证 | `mvn -q compile` + 全场景回归 + **灰度**：先切 10% 流量观察 2 天（指标：理解失败率、意图误映射率） |

### 任务 3.2 steps 结构 + 三项守门校验（DefaultUnderstander.java，+90 行）

| 校验 | 逻辑 | 失败动作 |
|---|---|---|
| ① inputFrom 静态校验 | `steps[].inputFrom.<下游参数>` 的引用键必须存在于上游工具 `getOutputFields()` 声明 | 剔除该映射（下游参数走常规 params）+ 日志告警 |
| ② DAG 无环校验 | steps 按 inputFrom 构建依赖图，拓扑排序检测环 | 拒绝整个计划 → 报错（诊断落盘） |
| ③ 一次重选 | sanitizeTools 过滤后为空 → 以“工具不存在，请从清单重选”回传 LLM 重试 1 次 | 仍失败 → 报错终止（不无限重试） |

| 项 | 内容 |
|---|---|
| LLM 输出扩展 | 可选 `steps: [{tool, inputFrom: {param: "<上游tool>.<输出键>"}}]`；不给 steps 时按 tools 顺序直跑（现有降级逻辑） |
| 执行层 | `ExecStep.java` / `DefaultExecutor.java` **零改动**（result: 数据流通道已有） |
| 验证 | 构造 3 个用例：合法 steps 计划执行成功；inputFrom 引用不存在键 → 剔除 + 降级；含环计划 → 拒绝 |

### 任务 3.3 体验指标埋点（前端 1 处）

| 项 | 内容 |
|---|---|
| 改动 | follow_ups 点击埋点（后端 CLARIFY 计数 `incrementClarifyRounds` 已有）；同时核查 `DefaultPresenter.suggestFollowUps`（:119-157）中的工具名写死分支（swrl_root_cause/swrl_risk_audit/sparql_query）——阶段三后由 LLM 基于工具描述生成跟进话术（方案 11.2 触点④），此函数改为回退路径 |
| 验证 | 埋点数据可查询；跟进话术所指工具在场景白名单内 |

**阶段三退出**：回归 + 灰度通过；11.4 指标对比基线无恶化（追问率应下降）。
**回滚**：`git revert` 恢复 switch 映射，阶段一/二成果不受影响。

---

## 阶段四：演示即真实（M4，依赖产品决策 0.2，预估 2 天）

> 以下按推荐方案 A 编制；若决策为 B/C 见任务 4.3。

**现状依赖核实**（实施前必读）：`RdAssistantPage.vue` 中 playbook 演示链路比方案预判更重——`playProductReply` 有 9 个调用点（:330/351/437/450/966/1028/1063/1115/1151），其中部分并非“静态剧本播放”，而是**真实操作后的结果回填展示**（如 :330-351 提交备案失败/成功回填、:427-442 查询结果点击后 copy-as-draft 回填）。这类调用依赖 `thinkingSchedule` 的场景模板对齐时间线，但内容来自真实后端操作。

**实施拆分**（防止误删真实功能）：

| # | 任务 | 判定 |
|---|---|---|
| 4.1 | **甄别两类调用点**：静态剧本播放（演示话术 → thinkingSchedule 演出）vs 真实回填（真实操作 → playbook 结果展示） | 逐个标注 9 个调用点；真实回填类**保留**，仅将时间线来源从 `createSchedule(scenario)` 换为真实 thinking 事件 |
| 4.2 | 演示话术场景改造：`resolveProductScenario`（:575-599）命中的 9 场景，点击后不再走 `simulateReply` 播放，改为向后端发真实请求、渲染真实 SSE；演示话术保留为输入预填 | `normalizePlaybookSteps`（:697-750）的模板对齐逻辑随 SSE 真实下发而退役 |
| 4.3 | 备选分支：产品选 B → `thinkingSchedule.js` + `simulateReply.js` 拆独立懒加载 chunk + “演示数据”水印标识；选 C → 删除演示入口（保留真实回填路径） | 按决策执行 |
| 4.4 | 删除 `thinkingSchedule.js`（366 行）+ `simulateReply.js` 中仅被演示使用的导出；相关测试用例同步删除 | `createStreamingPlaceholder`/`sleep` 若被真实链路共用则迁移至独立 util 文件后再删原文件 |
| 4.5 | 欢迎页边界确认：assistantModes 欢迎页保留（静态场景说明），确认无“演结果”内容混入 | 评审确认 |

**验证**：9 个研发场景逐一实测（演示入口 → 真实 SSE 渲染）；`npm run build` 通过。

---

## 阶段五：UI 交互改造（方案 11 章/11.6 缺口补齐，M5，预估 3~4 天）

> 定位：渲染层大改不需要（"只渲染后端下发数据"不变），本阶段只补齐方案 11.6 明确的三个能力缺口 + 一个后端生成升级。可与阶段四并行（改动面不重叠）。

### 任务 5.1 澄清追问"选择题化"（U1，方案 11.2 触点③）

| 项 | 内容 |
|---|---|
| 现状 | `ChatMessageList.vue:250-265` clarify-area 为纯文本框（`input` 开放问答），用户需"回答问题" |
| 前端改动 | clarify-field 渲染升级：ToolParam 契约下发**候选选项**（`options` 字段）时渲染为可点 chip（如时间给"本月/近7天/自定义"），点击即回填 `clarifyDrafts` 并可继续/自动提交；无候选时保留现有文本框——**渐进增强，不破坏现有补参流** |
| 后端 | `ToolParam` 增加可选 `options`（枚举候选）；`validateParams` 生成 CLARIFY 时随 `clarify` 一并下发候选列表 |
| 预估 | 前端 ~1 天 + 后端 ~0.5 天 |
| 验证 | R10 追加：有 options 的参数显示 chip 且点击即回填；无 options 的参数文本框行为不变 |

### 任务 5.2 CONFIRM 意图确认卡片（U2，方案 11.6(b) 新增交互）

| 项 | 内容 |
|---|---|
| 现状 | **无此分支**——意图模糊时理解层只能报错终止（`DefaultUnderstander:127-131`）或误映射 |
| 后端 | ① 理解层 prompt 增加输出约定：需求存在多种合理解读时输出 `{"intent": "CONFIRM", "candidates": ["解读1", "解读2", ...]}`；② `parseLlmResults` 新增 CONFIRM 分支：生成与 CLARIFY 同构的确认计划（不执行任何工具，整轮暂停）；③ **上限防死循环**：连续 2 轮 CONFIRM 未收敛 → 取最优解读继续执行 + 结论明示假设（复用 `exceedClarifyLimit` 语义，独立计数） |
| 前端 | `ChatMessageList.vue` 新增 confirm 区块：候选渲染为可点击卡片（复用 clarify-area 样式），点击即以该候选话术重发（走 `suggest` 通道）；与 CLARIFY 补参区互斥渲染 |
| 契约 | CONFIRM 与 CLARIFY 同构：`msg.confirm = {candidates: [...]}`，`isReplySettled` 后展示 |
| 预估 | 后端 ~1 天 + 前端 ~0.5 天 |
| 验证 | 新增用例 R14：输入歧义话术（如"看看畅享128"）→ 出候选卡片；点击候选 → 以该话术重发并正常执行；连续 2 轮不选 → 按最优解读执行且结论含假设说明 |

### 任务 5.3 假设透明回显（U3，方案 11.6(c) 回显义务）

| 项 | 内容 |
|---|---|
| 现状 | 超澄清上限按 defaultValue 静默继续（`DefaultUnderstander:204-209`），用户不知道结论前提 |
| 后端 | ① `SessionContext` 增加 `assumptions` 列表；凡"系统自行推断"的取值（defaultValue 回填、超限继续、L4 业务常识补全）写入记录（参数名+推断值+来源）；② E5 表达层 prompt 注入"请向用户说明以下推断假设"；③ 结论正文明示："本次按 [时间范围=本月] 推断执行，如不符请说明" |
| 前端 | **仅渲染**（时间线/正文透传，无新增组件）；思考时间线 goal 行可附带假设标注 |
| 预估 | 后端 ~1 天 + 前端 ~0.2 天 |
| 验证 | 新增用例 R15：归因不指明时间且澄清超限 → 结论含"按 [时间范围=本月] 推断"字样 |

### 任务 5.4 跟进话术任务链化（U4，方案 11.2 触点④，已在任务 3.3 挂靠）

| 项 | 内容 |
|---|---|
| 说明 | UI 通道（`ChatMessageList.vue:236-247` next-steps）现成，**前端 0 改动**；本任务确认后端生成逻辑归属：`suggestFollowUps` LLM 化时，prompt 要求基于"本轮工具结果 + 工具描述声明的业务承接关系"生成（归因→生成处置工单；稽核→导出清单；对比→写回草稿） |
| 守门 | 生成话术所指工具必须在本场景白名单内（复用 sanitizeTools 校验），非法候选剔除 |
| 验证 | R10/R7 场景后 nextSteps 为业务承接话术（非通用三句）；点击可直达下一轮 |

**阶段五退出**：R10（升级版）+ R14 + R15 通过；追问率/一次办结率对比 0.4 基线改善。

---

## 阶段四/五回归用例补充表

| # | 用例 | 操作 | 预期 | 覆盖任务 |
|---|---|---|---|---|
| R14 | CONFIRM 意图确认 | 输入歧义话术"看看畅享128" | 候选卡片展示；点击即重发执行；超限 2 轮后按最优解读+明示假设 | 任务 5.2 |
| R15 | 假设透明回显 | 归因缺时间且超限 | 结论明示"按 [时间范围=本月] 推断执行" | 任务 5.3 |
| R16 | 选择题化补参 | 触发含 options 参数的澄清 | chip 点击回填；无 options 参数文本框不变 | 任务 5.1 |

---

## 里程碑与风险

| 里程碑 | 内容 | 预估 | 风险 | 缓解 |
|---|---|---|---|---|
| M1 | 阶段一（前端去兜底） | T+2 天 | 低 | 三条前置已核实 |
| M2 | 阶段二（prompt 动态化 + 文案 LLM 化） | T+5 天 | 低 | 20 条话术输出对比；独立 revert |
| M3 | 阶段三（映射退役 + steps 守门） | T+9 天 | **中** | 灰度 10%×2 天；含环/幻觉用例覆盖；回归不过即 revert |
| M4 | 阶段四（演示即真实） | T+11 天 | 中（playbook 真实回填误删风险） | 任务 4.1 甄别两类调用点后再动手 |
| M5 | 阶段五（UI 交互：选择题化/CONFIRM/假设透明） | T+15 天（可与 M4 并行） | 低（渐进增强，不破坏现有补参流） | CONFIRM/CLARIFY 互斥渲染；独立计数防死循环；前端仅渲染假设标注 |

**全程红线**（评审检查）：
1. 不得为单个场景写编排代码（`if (scene == X) 专用编排` 即打回）
2. 不得新增第二套流程描述格式（YAML/DSL 流程文件均视为双轨）
3. 单轮 LLM 调用 ≤3 次（理解 + 表达 + 最多一次重选）
4. 任何新场景改动点数 > 3 处即打回（方案 6.1）
5. 前端禁止出现 `toolName === 'xxx'` 类工具名分支（方案 6.3）

---

## 与设计文档的差异记录（实施中发现，需回写方案）

1. `RdAssistantPage.vue` 的 playbook 调用含“真实操作结果回填”类（非纯演示），批次③删除范围需按任务 4.1 甄别结果收窄——方案 4.1/4.3 的“三选一”表述需补充此约束
2. `DefaultPresenter.suggestFollowUps` 存在工具名写死分支（:119-157），方案第 8 章病灶表未单列——已纳入任务 3.3 一并 LLM 化
3. 方案引用行号与 2026-08-31 基线偏移（如 `DefaultUnderstander` 方案标 561 行中事件区间为 :490-537，实际 prompt 段为 :485-537），本文档行号为实施基准
4. 方案 11.6 定义的三项能力缺口（选择题化补参/CONFIRM 意图确认/假设透明回显）未纳入原方案第 5 章实施步骤——已补充为本文档阶段五（任务 5.1~5.3），并新增回归用例 R14~R16
