# 产商品研发助手能力矩阵

> 对照 [`scene_mapping.json`](../backend-app/src/main/resources/scenes/scene_mapping.json) 与 `/api/v1/product-ontology`  
> 状态：`已有` = 联调可用 · `demo-only` = 依赖 mock/内存图 · `缺口` = 待建设  
> 更新日期：2026-07-26

## 1. 场景能力

| 能力 | 场景码 | API | 状态 | 说明 |
|------|--------|-----|------|------|
| 智聊·对话配置 | `offering_config_chat` | `POST /config/chat` | 已有 | 槽位抽取 + 推理补全 + 合规 |
| 智读·文件配置 | `offering_config_batch` | `POST /config/batch`、`/config/batch-upload` | 已有 / demo-only | 文本已通；Office 解析有引擎，回归集刚补齐 |
| 智检·合规校验 | `offering_config_compliance` | `POST /config/compliance` | 已有 | 在架 / 草稿双源 |
| 智查·历史复用 | （快捷 rd.query） | `POST /config/discover`、`/config/copy-as-draft` | 已有 / demo-only | 命中依赖事实图数据源 |
| 字段推理 | — | `POST /config/infer` | 已有 | 场景缺省 / 模板 |
| 配置追溯 | — | `GET /config/trace`、`POST /config/explain` | 已有 | 内存 trace，进程重启丢失 |
| 知识沉淀 | — | `POST /config/publish` | 已有 / demo-only | 写入内存图 + RDF，非主数据系统 |
| 草稿持久化 | — | `GET/POST/PUT/DELETE /config/drafts` | 已有 | JPA `ontology_instance`，按会话恢复 |
| 提交/备案闭环 | — | `POST /config/submit` | 已有 | 合规 → 沉淀 → `rd_filing` 备案工单 |
| 多方案对比 | `offering_config_compare` | `POST /config/compare` | 已有 | 合规 + 收益估算 + 推荐说明 + 右侧面板 |
| 资费备案工作流 | `tariff_filing_apply` | workflow | demo-only / 缺口 | 场景部分未激活，未与研发助手一键串联 |
| 真实主数据同步 | — | — | 缺口 | BOSS/CRM T+1 未接入 |
| Function Calling Agent | — | ToolConfig / MCP | 缺口 | 进度报告规划中 |
| RAG 制度库 | — | — | 缺口 | 未建设 |
| 规则运营后台 | — | `ops/risk-rules` 部分 | 缺口 | 配置规则仍偏代码/JSON |

## 2. 前端入口

| 模块 | 路径 | 状态 |
|------|------|------|
| 研发助手页 | `frontend/src/components/RdAssistantPage.vue` | 已有 |
| 配置状态 | `frontend/src/composables/useProductConfig.js` | 已有（统一走 product-ontology） |
| API 封装 | `frontend/src/services/productOntologyApi.js` | 已有 |
| 本地可视化链 | `productOntologyLocal.js` | demo-only（仅展示，不做业务兜底） |
| mock 商品样本 | `productMockData.js` | demo-only（表单 schema 仍复用） |

## 3. 闭环对照（目标 vs 现状）

```
发现历史 → 智聊/智读 → 草稿持久化 → 智检 → 提交备案/工单 → 智查可复用
   ✅         ✅           ✅          ✅        ✅              ⚠️(内存图)
```

## 4. 下一优先项（矩阵外延）

1. 事实图与主数据双向同步，去掉 demoMode 依赖  
2. 配置规则热加载与版本审计 UI  
3. 备案工作流与 `tariff_filing_apply` 正式串联  
4. Agent 工具化（infer/compliance/discover/submit）
