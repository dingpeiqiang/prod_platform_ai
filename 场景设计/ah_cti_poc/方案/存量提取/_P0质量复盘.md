# P0 质量复盘（900113046 融合199）V1.0

> 数据：报文 3.2KB | 有值字段 32（存量提取 31 + 默认值 1）| enum_violation 9 项 | pending 34/64（53%）| 渲染 PASS

## 1. enum_violation 9 项逐条处置（评审结论#4：校验不改写，人工定口径）

| # | 路径 | 提取值 | schema enum | 处置口径 |
| --- | --- | --- | --- | --- |
| 1 | baseInfo.prodPrcName | 5G-A融合套餐199元 | ["按资费表字符长度限制"] | **误报**——enum 列实为"取值说明"（非封闭枚举），资费名称本自由文本。→ 待办：schema 转换器对"取值说明型单值 enum"降级为 description（影响全部 6 模板同名形态） |
| 2 | baseInfo.dateLimit | 24个月（自动续展…） | ["默认为36","置灰不可修改"] | **半误报**——enum 是格式说明；实际值应为纯数字"24"。→ 提取口径修正：协议期只取数字 |
| 3 | baseInfo.roleMax | 6 | ["1-9之间正整数"] | **误报**——enum 是范围说明。→ 同#1，范围型说明降级为 x-format 提示 |
| 4 | phoneMbrInfo.ifChangeBasePrc | 允许 | ["是、否"] | **提取错**——应为"是"。→ 提示词补规则：是否类字段归一"是/否" |
| 5 | phoneMbrInfo.phoneMbrMax | 3 | ["1-6之间整数"] | **误报**——同#3 |
| 6 | phoneMbrInfo.freeMbrCnt | 2 | ["1-6之间整数"] | **误报**——同#3 |
| 7 | acctMonth.calcMode | 周期型缴费：月 | 4 项封闭枚举 | **真实违例**——原文口径模板未覆盖。→ 留原文+violation 记录（业务确认映射，不改写） |
| 8 | billGprsCfg.resourceType | 国内通用流量 | ["国内-通用-单月"] | **真实违例**——码表值差异。→ 同#7 留原文 |
| 9 | billVoiceCfg.resourceType | 国内语音 | ["国内主叫"] | **真实违例**——同#7 |

**结论**：9 项中 6 项为 schema 转换器的"说明型 enum"误报（需降级改造 excel_to_schema.py），3 项为真实码表违例（保留原文进异常清单人工确认）。

## 2. pending 53% 处置（阈值决策）

- 模板必填 64 项中 34 项无值——**符合预期**：K4 md 是"需求单"语料，不是"配置单"语料（编码类/日期类/短信文案类字段正文天然没有）；
- 存量实例化场景 pending 语义 ≠ 新需求出口A（不阻断），改为 `_report.json` 的"平台配置待补项"统计维度；
- **阈值调整**：存量场景提取质量门禁改为"**正文可提取命中率**"（正文提及的字段被正确提取的比例），P0 实测 32/(32+34)≈48% 需按模板分类统计后定阈值，新增"可提取必填"白名单概念（如 effDate/expDate/编码类不可提取，不计入分母）。

## 3. P0 链路验证结论

- 提示词（extract-prompt-template.md 存量附加规则）→ 提取 → merge --mode legacy → render 全链路打通；
- 渲染表格业务可读（概览卡片+分节），待补充区语义正确；
- 遗留改造项（进 S3）：
  1. excel_to_schema.py：说明型 enum 降级（prodPrcName/roleMax/dateLimit 等形态识别）；
  2. validate_elements.py：是否类字段归一检查 + 可提取必填白名单机制；
  3. merge_nested.py：enum_violation 增"范围说明型"豁免（x-format 提示不参与校验）。

## 4. 产出物

- 存量提取/_extract_900113046.json（提取要素，审计留档）
- 存量提取/_merge_900113046.json（合并全出参含 _meta）
- 存量提取/900113046.json（实例化报文）
- 存量提取/900113046.md（渲染表格）

## 5. S3 改造后回归（本轮完成）

- S3a 说明型 enum 降级：6 模板 enum 210→182 节点、x-hint 0→28；skill 区 schema 已同步逐字节一致；
- S3b merge_nested 豁免说明型 enum：P0 重跑 violation 9→4（消除 prodPrcName/dateLimit/roleMax/phoneMbrMax/freeMbrCnt 5 项误报），payload 逐字节一致；
- S3c validate_elements.py 新建：六项校验（路径/数值/枚举/是否类归一提示/可提取命中率门禁/价格交叉），P0 rate 0.4902 ≥ 0.30 PASS；
- S3d 剩余 4 项 violation = 3 真实码表违例（calcMode/billGprsCfg/billVoiceCfg，留原文人工确认）+ 1 提取错（ifChangeBasePrc "允许"，提示词归一口径"是否类→是/否"）。

## 6. P1~P3 批量复盘（18/18 全量完成）

| 批次 | 范围 | 结果 |
| --- | --- | --- |
| P0 | 900113046 融合199（人工核对版） | violation 4（3 真实违例 + 1 提取错），rate 0.4902 |
| P1 | 900102308 单品199 | PASS，violation 0，rate 0.3714 |
| P2 | 900117020 权益包19.9 | PASS（threshold=0.20），violation 0 |
| P3 | 其余 12 active + dup 900102306 复制 | 17 active 全 PASS（familyBasePrc 3 条补齐副卡/总述节后 rate 0.3725），dup 复制+标记 |

**阈值口径（按模板分组，写入 _report.json）**：
- personMainPrc / familyBasePrc：threshold=0.30（主套餐语料正文可提取维度充足）；
- personAddPrc：threshold=0.20（权益包产品形态不适用优惠/保底/语音/短信 4 组必填，实际可提取分母约 22 项）。

**P1~P3 提取模式说明**：主套餐 10 条为同构 5G-A 语料（仅档位/资源/速率/卫星/宽带/高清/文号差异），权益包 8 条为同构权益随心选语料（仅版本/月费/流量差异），批量提取由确定性生成器完成（K4 正文事实数据，legacy 模式价格豁免）。

**产出物归档**：
- skill/references/K5存量报文/：18 × (.json 报文 + .md 渲染表格) + _report.json；
- 方案/存量提取/：18 × _extract_*.json + _merge_*.json（审计留档）。
