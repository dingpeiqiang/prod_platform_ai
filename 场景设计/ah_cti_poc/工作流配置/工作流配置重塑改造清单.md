# 工作流配置重塑改造清单（废弃 skill → 工作流配置）

> 目的：废弃 `skills/cpcp-product-worker` 技能包，回到并**重塑** `工作流配置`（V1.6 基线），
> 把 skill 积累的新能力**倒灌**回工作流配置，避免能力倒退。
> skill 能力载体对照：SKILL.md + flow-A0/A/B/C/D + dispatcher.py + 35 个 cpcp_api 子命令 +
> run_pipeline/run_requirement 状态机 + 6 套 schema 模板 + 5 类知识库。

## 一、目标形态（重塑后工作流总览）

| 序号 | 工作流 | 动作 | 覆盖环节 | 对应 skill |
|:--:|------|:--:|:--:|------|
| wf_main_intent | 意图调度主流程 | **新增** | 入口(0) | dispatcher.py + 意图路由表 |
| wf_sub_00 | 需求提报 | **新增** | 环节1 | flow-A0 + render_requirement_report |
| wf_sub_01 | 需求分析 | **重写模板轨** | 环节2 | flow-A(V7.0) |
| wf_sub_02 | 智能配置 | **扩展融合组** | 环节3 | flow-B 环节1 |
| wf_sub_03 | 规格稽核 | **扩展融合组** | 环节4 | flow-B 环节2 |
| wf_sub_04 | 自动测试 | **重写** | 环节6/7 | flow-B 环节4 + 受理验证 |
| wf_sub_05 | 资费校准 | **扩展融合组** | 环节5 | flow-B 环节3 |
| wf_sub_06 | 上线审批 | **扩展双轨+自动上线** | 环节8/9 | flow-C + flow-D D-1 |
| wf_sub_07 | 监控运维 | **扩展根因闭环** | 环节9 | flow-D D-2 |
| wf_sub_08 | 审批进度查询 | 微调支持双轨 | 环节8查询 | flow-D D-1 |
| wf_sub_09 | 存量产品查询 | **新增** | 只读 | flow-D D-4 |
| wf_sub_10 | 存量合规扫描 | **新增(可选)** | 只读 | shelf_compliance |

## 二、能力差距核对表（skill 有 → 工作流无）

| # | 能力 | skill 载体 | 工作流现状 | 改造动作 |
|:--:|------|-----------|-----------|---------|
| 1 | 意图路由/澄清(12 intent/查询or配置) | dispatcher.py | 仅LLM调度口头约定 | → wf_main_intent |
| 2 | 需求提报+需求工单审批门禁 | flow-A0 | 无 | → wf_sub_00 |
| 3 | 模板轨需求分析(6模板×24字段/merge_nested/validate_nested本体闸/explain) | flow-A | wf_sub_01 旧18字段 | → 重写 wf_sub_01 |
| 4 | 审批双轨(requirement/launch) | submit_approval | 仅 launch | → 扩展 wf_sub_06/08 |
| 5 | 融合商品(组结构) config/spec/fee/test 组维度 | flow-B V4.0 | 无 | → 扩展 02/03/04/05 |
| 6 | 31条固定用例报告(ACC/BILL/CUST)+P0/P1/P2+9章节 | flow-B 环节4 | wf_sub_04 18字段旧报告 | → 重写 wf_sub_04 |
| 7 | 存量产品查询(只读) | query_offer | 无 | → wf_sub_09 |
| 8 | 存量合规扫描 | shelf_compliance | 无 | → wf_sub_10 |
| 9 | 监控根因本体推理+建工单闭环 | ops_root_cause/create_work_order | wf_sub_07 仅查询告警 | → 扩展 wf_sub_07 |
| 10 | 审批通过自动上线+监控运维方案+xsbot-panel | flow-D V13.0 | wf_sub_06 停审批推送 | → 扩展 wf_sub_06 |

## 三、实施步骤清单（按依赖顺序）

### 阶段 0：资产迁移准备
- [x] 0.1 将 6 套 schema 模板（`scripts/templates/*.schema.json` + `_registry.json`）迁入工作流配置可引用目录（方案/templates 已确认存在且与 skill 源 _registry.json 逐字节一致；新增 `_index.json`）
- [x] 0.2 将 K1~K5 知识库、`存量产品目录_清洗后.json`、`ontology-fields.json`、`seed_offer_groups.json` 随包迁移/登记
      > 已迁入 `knowledge/`：K1规范/K2资费/K3测试(V2.0，V1.0 已删)/K4存量(18份)/K5FAQ/K5存量报文(18×2+report)/ontology-fields.json|md/seed_offer_groups.json/存量产品目录_清洗后.json/README_知识库总索引.md(已更新登记新增资产)/gen_k4_slices.py
- [x] 0.3 统一版本命名：清掉 "V1.6"/"V1.7" 混杂，改为单一版本（如 V2.0）
      > 生成器头统一为 V2.0；`gen_workflows.py`（V1.7 基线）作为被 import 的基础生成器保留其版本注释以明确基线；目录名 `智能体工作流集V1.6` 保持不改（避免破坏工作流/导入路径），版本语义以文件头 V2.0 为准

### 阶段 1：插件/代码节点层（先让原子能力可用）
- [x] 1.1 **新增插件（Java后端接口）**：
      - `validate_nested`（本体校验闸，R-C04/C06，backend 6174）
      - `explain_nested`（推理依据，/config/explain + /config/provenance）
      - `ops_root_cause`（异动根因本体推理，复用 product-ops.ttl）
      - `create_work_order` / `query_work_order` / `update_work_order`（工单闭环）
      - `shelf_compliance`（存量合规扫描）
      - `query_offer`（存量检索；本地只读，无需后端化）
      > 进度：后端 `backend-app` ProductOntologyController 的能力端点**已全部存在**（`/api/v1/product-ontology/config/validate-nested`、`/config/explain`、`/ops/root-cause`、`/ops/work-orders`、`/config/shelf-compliance`）。
      > 已推进（阶段1）：4 个占位代码节点已改为 **urllib POST 调用 appstore 网关对应端点**，端点不可达时优雅回退 `backend_pending=1`（离线 Demo/诚实占位口径不变），后端就绪后真实数据直接流入：
      >   - wf_sub_07 `CODE_OP_ROOT_CAUSE`(706) → `POST {BASE_URL}/api/v1/appstore/ops/root-cause`
      >   - wf_sub_07 `CODE_OP_CREATE_WO`(708) → `POST {BASE_URL}/api/v1/appstore/ops/work-orders`
      >   - wf_sub_10 `CODE_OP_SHELF_COMPLIANCE`(1002) → `POST {BASE_URL}/api/v1/appstore/shelf-compliance`
      >   - wf_sub_01 **新增 `CODE_OP_VALIDATE_NESTED` 代码节点(109)** → `POST {BASE_URL}/api/v1/appstore/validate-nested`（输入 payload+template_id，出参 backend_pending/valid/error_list/explain/note，优雅回退）；已插入 wf_sub_01（merge_nested 108 之后、render_table 之前），原 render_table 109→**110**、save 110→**111**、end 111→**112** 重编号，e01 边更新为 101→…→112，模板新增"【本体校验】valid={valid…}…”行。`CODE_OP_VALIDATE_NESTED` 常量定义在 `PLAN_ELICIT_PROMPT` 后、`s01=[]` 前（前向引用修正，避免 NameError 生成器中断）。
      > 已闭环（后端）：后端 `AppStoreV16Controller` 已新增 7 个 `/api/v1/appstore/*` 工作流适配端点并经编译验证、提交（commit ee6f5a8）：`POST /ops/root-cause`、`POST /ops/work-orders`(work_order_id 取嵌套 workOrder.workOrderId)、`POST /shelf-compliance`(items→rows/results)、`POST /validate-nested`(pass→valid"1"/"0"、violations→error_list、explain_hint→explain)、`POST /explain`(explanation→explain)、`POST /report/download`(回退固有 GET /test/offer/report 拼 download_url)、`POST /script/download`(回退固有 GET /product/config/script 拼 download_url)；均含 `backend_pending=0/1` 优雅回退口径。
      > 待验（网关）：工作流经网关 10.86.13.201:31281 调用，需确认网关已把 `/api/v1/appstore/*` 代理到后端 6174，否则仍走网关旧逻辑（后端侧已就绪，网关属独立配置）。
- [x] 1.2 **新增插件（HTTP接口，独立或复用现有）**：
      - `download_launch_script`（配置脚本下载）
      - `download_test_report`（测试报告下载）
      > 已推进（本次）：新增 `CODE_DOWNLOAD_TEST_REPORT`（wf_sub_04 节点316，`POST {BASE_URL}/api/v1/appstore/report/download`，报告存储 308 后、结束 307 前）与 `CODE_DOWNLOAD_LAUNCH_SCRIPT`（wf_sub_06 节点621，`POST {BASE_URL}/api/v1/appstore/script/download`，审批通过分支 618 监控运维方案后、结束 619 前）；均 urllib POST + 端点不可达回退下载引导文本（`backend_pending=1`、download_url 空）。重建后 12 工作流全量校验 ALL_OK（wf_sub_04 16节点、wf_sub_06 21节点）。后端对应端点 `POST /report/download`、`POST /script/download` 已在 1.1 一并补齐（commit ee6f5a8），后端就绪后地址直接流入。
- [x] 1.3 **抽为 type=6 代码节点（纯本地确定性逻辑，从 skill 脚本直搬）**：
      - `merge_nested`、`render_table`、`validate_elements`、`derive_flat24`、`get_template`（读注册表）
      - `build_plan`、`extract_record`、`map_fixed_cases`
      - `render_requirement_report`（需求提报单渲染）
      > 注：`render_requirement_report`/`merge_nested`/`render_table`/`get_template` 已抽为内嵌代码节点并在 wf_sub_00/01 使用（gen_workflows_v2.py，去 argparse 改 async main(args)+Output 签名）；`extract_record`/`map_fixed_cases` 已分别用于 wf_sub_01/04；`validate_elements` **本次已抽为 `CODE_VALIDATE_ELEMENTS`**（源 skill validate_elements.py 六项校验：非法路径剔除/数值合法/是否类归一提示/可提取命中率质量门禁 E31/价格交叉核对，去文件IO改 schema_json+elements_json 入参），已接入 wf_sub_01 作为**新节点 108**（要素提取107 后、merge_nested 前），原 merge_nested 108→109、validate_nested 109→110、render_table 110→111、save 111→112、end 112→**113** 重编号、e01 边与结束模板更新（wf_sub_01 现 13 节点/12 边）；wf_sub_07/10 的 `CODE_OP_ROOT_CAUSE`/`CODE_OP_CREATE_WO`/`CODE_OP_SHELF_COMPLIANCE` 已由纯占位改为 HTTP 调用 appstore 端点（见 1.1）。
      > 待定：`derive_flat24`（24字段扁平表）与 `build_plan`（plan_json/plan_md）在现有 v2 工作流中已被等价节点取代（wf_sub_01 用 merge_nested+render_table、wf_sub_02 用 save_product_config 出 plan_json），按 YAGNI 收口：现有 v2 工作流（wf_sub_01 merge_nested+render_table、wf_sub_02 save_product_config）已实现等价功能，不再单独抽取接入，避免能力重复与链路冲突（用户已确认）。
- [x] 1.4  校准各插件出参 item 树（对齐 gen_workflows.py 的 `ARRAY_ITEM_FIELDS`/`TEST_POINT_FIELDS`）
      > 已推进（本次）：核对 v2 所有插件 array 出参 item 树，`realtime_spec_audit|error_list`(202)/`check_billing_rule|risk_list`(402)/`get_test_scenes|testScenes`(303)/`get_test_result|testScenes`(305，含 testCasePointResults→TEST_POINT_FIELDS 二层嵌套)/`query_product_monitor|alarm_list`(702) 均已就位；唯一缺失的 `query_approval_status|approval_matrix`(802) 已按 flow-C 审批矩阵口径在 gen_workflows.py `ARRAY_ITEM_FIELDS` 补齐 item 树（node_seq/node_name/approver/status/opinion/update_time）。重建后 12 工作流全量校验 ALL_OK。

### 阶段 2：子工作流改造（打通环节1→2→3）
- [x] 2.1 **新增 wf_sub_00 需求提报**：需求文本→LLM抽取需求字段(单/融合分支) → render_requirement_report 代码节点 → 待补充判定 → 发起需求工单审批(approval-type=requirement) → 审批门禁衔接
      > 已产出 `wf_sub_00_需求提报.json`（gen_workflows_v2.py；需求字段抽取LLM→render代码节点生成req_id+待补充判定→保存需求工单requirement_report→需求工单审批双轨之requirement轨→结束；有待补充走补充提示分支）
- [x] 2.2 **重写 wf_sub_01 需求分析（模板轨）**：
      产品识别(LLM) → 双源相似检索(query_offer 本地优先→similar_offer 兜底) → 取模板 → 要素提取(LLM)
      → merge_nested 合并 → validate_nested 本体闸+explain → derive_flat24 → render_table → 保存 requirement
      （多产品无循环节点 → 单产品闭合 或 按产品展开并行链）
      > 已产出 `wf_sub_01_需求分析.json`（含 产品识别→相似检索similar_offer→get_template→要素提取→merge_nested→render_table→保存requirement；validate_nested 本体闸为 1.1 Java 插件待后端就绪后插入；单产品闭合）
- [x] 2.3 重写后环节1 req_id 传入环节2 延续（不再自生成）
      > wf_sub_01 单入参 req_id 由环节1（wf_sub_00）延续，节点110 保存 requirement 沿用 req_id 不再自生成（与 wf_sub_02~05 自查链路口径一致）

### 阶段 3：执行主干融合组扩展
- [x] 3.1 wf_sub_02 智能配置：组结构 plan_json 时输出组回显行（role/offer_id），成员 PARTIAL → 整体 PARTIAL
      > `wf_sub_02_智能配置.json` 新增 `融合成员回显` 代码节点（CODE_FUSION_GROUP_ECHO）：出参含 group 时逐字生成 `融合成员：主 offer_id X / role（offer_id）...` 回显行并交结束节点；单商品（无 group）回显行空串、路径零变化。功能实测通过（融合/单商品双路径）
- [x] 3.2 wf_sub_03 规格稽核：组维度稽核（offer_group_check），error_list 按 member_role 定位
      > `wf_sub_03_规格稽核.json` 整改建议生成 LLM 提示词增加组维度：出参含 group 时追加 `> 组维度：主 offer_id {offer_id} + 成员 role（offer_id）/...` 回显；error_list 含 `group:<role>` 类目时对应用户成员行标 ❌ 附明细；单商品零变化
- [x] 3.3 wf_sub_05 资费校准：比对表按 member_role 分组、E27 阈值逐成员内计算
      > `wf_sub_05_资费校准.json` 风险解读 LLM 提示词增加融合组：compare_list 含 member_role 时按成员分组输出比对表（8 项项目名不变，行数=Σ各成员有值行）、空值省略逐成员内执行、E27 阈值逐成员内计算（禁跨成员加总）；单商品零变化

### 阶段 4：自动测试重写（正式版报告）
- [x] 4.1 **重写 wf_sub_04**：发起测试→查询场景→代码节点轮询进度→查询结果→
      **被测一致性预校验(E26)** → **场景覆盖核对** → **map_fixed_cases(31条/ACC-BILL-CUST/P0-P1-P2)** →
      9章节正式版报告生成 → 报告下载 → 存储 test
      > 已产出 `wf_sub_04_自动测试.json`（gen_workflows_v2.py 阶段4 重写，15节点/14边）：
      > 链路 301开始→309读config→310提取原文+offer_id→302发起测试→303查询场景→311/312自检spec(node_name=spec)→313/314自检fee(node_name=fee)→304轮询(CODE_POLL_PROGRESS)→305查询结果(testScenes/orderId/offerInstId/offerName)→315 CODE_MAP_FIXED_CASES 确定性构建31条固定用例(ACC/BILL/CUST 含✅/❌/本销售品未覆盖判定，逐字引用于 skill FIXED_CASES)+P0/P1统计+整体上线结论三选一+K3 规范6 判定+缺陷清单+K3 第5章场景覆盖核对+E26 被测一致性核对(主 offerName 一致+融合组 offer_group_check 成员角色集合 vs plan member_offers)→306 LLM 按 K3 模板 V2.0 九章节渲染《销售品自动化测试报告》正式版(受理验证独立成节)→308存储 node_name=test→307结束。
      > 功能实测（单商品全通过→✅建议上线/P0失败→❌禁止上线/risk非空→⚠️评估风险后上线；E26 主商品与融合组成员角色不一致均正确置 e26=0；场景覆盖缺 S_ADD_CARD 自动降级⚠️）
- [x] 4.2 **受理验证独立成节（环节7 标题头）**，随环节6 同块展示
      > wf_sub_04 节点306 报告生成提示词内四章分项约束"受理验证独立成节（标题'环节7/9·受理验证'，逐字引用 orderId/offerInstId 与逐受理场景 S_O_TC/S_ADD_CARD/S_U_TC 结论与关键测点，融合品含 S_GROUP_BIND/S_ADDON_SUB）"，E14 凭证为空按固定文案标注"未获取到受理凭证，需人工核实"不中断

### 阶段 5：审批/监控/存量
- [x] 5.1 wf_sub_06 上线审批：支持 approval-type 双轨；审批通过 → **自动衔接监控运维方案 + xsbot-panel 看板**
      > `wf_sub_06_上线审批.json`（gen_workflows_v2.py 阶段5 重写，20节点/19边）：五环节自查(config/spec/fee/test/requirement，各配提取代码节点)→CODE_SUMMARY_APPROVAL 合成结构化汇总→LLM 生成《上线审批建议》(环节8标题头+校验看板+风险+整体结论)→存储 report→submit_release_approval 推送(approval-type=launch，后端硬门禁 approve_confirmed=true+req_id四环节齐全)→CODE_APPROVAL_POLL 轮询审批状态(模拟10s后自动通过)→selector 按 status=通过 分流：通过→LLM 生成监控运维方案(环节9标题头+xsbot-panel看板)并自动上线结束(无需用户回复【确认上线】)；待审/驳回→环节8收尾块结束(引导【查询审批进度】核验)
- [x] 5.2 wf_sub_07 监控运维：异常分支追加 ops_root_cause 根因推理链 + create_work_order 建工单闭环
      > `wf_sub_07_监控运维.json`（阶段5 重写，11节点/10边）：query_product_monitor→异常判定(error_count≠0)→异常分支(告警文案→send_alert→ops_root_cause 根因推理[阶段1.1 Java插件待接入占位]→LLM 根因推理链+优化方案→create_work_order 建工单闭环[占位]→结束异常闭环+xsbot-panel+收尾块)；正常分支(运营摘要→结束正常+xsbot-panel+收尾块)
- [x] 5.3 wf_sub_08 审批进度查询：approval-type 区分两类审批回显
      > `wf_sub_08_审批进度查询.json`（阶段5 重写，4节点/3边）：query_approval_status(approval_id 优先/offer_id 兜底，V9.1 双轨)→LLM 状态摘要归纳（固定格式+审批矩阵逐字引用+按 approval_type 区分回显：requirement→引导需求分析【开始配置】/launch→通过即自动上线衔监控运维）+收尾固定块
- [x] 5.4 **新增 wf_sub_09 存量产品查询**：query_offer 只读 + K4 档案回显 + 多命中收敛
      > `wf_sub_09_存量产品查询.json`（阶段5 新增，4节点/3边）：CODE_OP_QUERY_OFFER 内嵌确定性检索（内嵌 knowledge/存量产品目录_清洗后.json 18 条目录，便于 Demo；status=dup 按 duplicate_of 透传；后端 query_offer 插件就绪后切换远端检索，K4 档案待后端回显）→LLM 按 flow-D D-4 渲染查询回显（unique 结构化回显+收尾块 / multi 匹配清单收敛 / none 追问引导）。只读、不生成 req_id
- [x] 5.5 **新增 wf_sub_10 存量合规扫描**：shelf_compliance 批量 R-C* 合规 + 整改引导
      > `wf_sub_10_存量合规扫描.json`（阶段5 新增(可选)，4节点/3边）：CODE_OP_SHELF_COMPLIANCE 占位(阶段1.1 Java 插件待就绪)→LLM 合规报告渲染（R-C* 合规结论+整改引导）

### 阶段 6：意图调度收口 + 清理
- [x] 6.1 **新增 wf_main_intent 意图调度**：dispatcher 代码节点 → 条件分支 → 按 intent 路由到各子流（含 ASK_INTENT 澄清）
      > `wf_main_intent_意图调度.json`（18节点/17边，1 个 CODE_DISPATCHER 代码节点）：封闭枚举12意图+确认门禁+实体抽取+产品文档/描述判定+KB分流+LLM最小化兜底+OUT_OF_SCOPE 关键字；路由级联 REQ_REPORT→wf_sub_00 / APPROVAL→wf_sub_06 / QUERY_APPROVAL→wf_sub_08 / QUERY_MONITOR→wf_sub_07 / QUERY_OFFER→wf_sub_09 / CONFIRM_EXEC|RESUME_EXEC→wf_merged_exec / 其余→LLM收口应答。CODE_DISPATCHER 已独立实测多种意图判定正确。
- [x] 6.2 清理：删除 `skills/cpcp-product-worker` 目录、`_patch_main2.py`（引用已删主流程的死代码）、废弃 V1.x 命名
      > 已完成：① 知识资产迁移 `knowledge/`（详见 0.2）并登记索引；② 代码节点/提示词中指向 `references/` 的数据源更新为 `knowledge/`（重建后 12 个工作流全量校验 ALL_OK、干净）；③ 删除 `skills/cpcp-product-worker/`、`skills/cpcp-product-worker.zip`、`skills/README.md`、`_patch_main2.py`；④ 版本命名统一 V2.0（见 0.3）

### 阶段 7：节点精简与执行工具
- [x] 7.1 **精简 wf_sub_01 需求分析**：13 → 11 节点（合并 `CODE_EXTRACT_MERGE`，删除无用节点 `CODE_OP_VALIDATE_NESTED` 及其冗余出参）
      > 合并要素提取与 merge_nested 为单一代码节点；同步清理全量工作流中的**状态类无用出参共 66 项**（仅删状态类，保留业务出参；`pending_count`/`status` 因条件分支依赖保留）。手册 `## 2` 章节已更新为 11 节点 + 精简说明。
- [x] 7.2 **精简 wf_sub_04 自动测试**：16 → 11 节点（删除 303/311/312/313/314，移除 315 的 `spec_record`/`fee_record` 等入参）
      > 下游契约不变（`save_node_result` 仍以 `node_name=requirement/config/spec/fee/test` + `req_id` 存储；wf_sub_02~06 按 `req_id+node_name` 读取）。手册 `## 5` 章节已更新为 11 节点 + 精简说明（5.4）。
- [x] 7.3 **新增工作流执行工具 `wf_runner`**：本地按 JSON 逐节点执行、显性打印每环节 输入/输出
      > 路径：`工作流配置/wf_runner/wf_runner.py`（单文件，约 1200 行）。定位：**逻辑与工作流 JSON 完全一致**的模拟执行器，用于本地验证与链路演示。
      >
      > **能力**
      > - 加载 JSON → 拓扑遍历 → 逐节点执行，彩色分节打印 `INPUT`/`OUTPUT`，可选 `--save-json` 落盘
      > - **LLM 节点**：实时调用（复用项目 LLM 配置），不可达回退 mock
      > - **插件节点（type=3）**：真实 HTTP GET/POST 调网关，失败回退 mock
      > - **代码节点（type=6）**：真实 `exec` JSON 内联 `async def main(args)` 源码
      > - 条件分支求值选边、结束节点模板渲染、子流程递归
      >
      > **LLM 配置来源**：读取 `backend-app/src/main/resources/sql/h2/data-h2.sql` 中 `pd_ai_llm_user_configs`（`is_active=1`）。当前激活模型 `qwen3-30b-a3b`，`base_url=https://aicp.teamshub.com/openai/api/v1/openai/v1`，`auth_type=custom`+`auth_header`（teamshub 网关 token），`is_full_url=1`，temperature 0.3 / max_tokens 4096。
      >
      > **CLI**
      > ```
      > wf_runner.py <workflow> [--set K=V] [--offline] [--fast-forward] [--no-color]
      >              [--no-values] [--save-json F] [--base-url U] [--model M] [--timeout S] [--list]
      > ```
      >
      > **验证结论**
      > - `--list` 正常列出 12 个子工作流（wf_sub_00~wf_sub_11）
      > - **离线批量**：12 个工作流全部 `rc=0`、错误 0、无超时（快进沙箱收敛轮询节点）
      > - wf_sub_01 离线：11 节点全流程跑通，验证/渲染/结束节点输出完整方案；wf_sub_04 离线：11 节点全跑通
      > - **真实联网** wf_sub_01：11/11 OK，含真实 LLM 调用（592ms/6218ms）与真实插件（similar 返回 900102308、save 返回真实 record_id）
      > - 网关 `http://10.86.13.201:31281` 实测可达；`product/config/save` 因无真实上游数据返回空（预期）
      >
      > **离线快进沙箱**：wf_sub_04 节点 304（`asyncio.sleep(5)`×360 轮询）、wf_sub_06 节点 0616（审批轮询）离线时需快进使一步收敛（progress→`done=true`，approval→`status=approved`）；实现方式为 exec 期间就地 monkeypatch `asyncio.sleep`/`time.sleep`/`urllib.request.urlopen` 并精确还原（`_Patch` 类），避免 import hook 破坏解释器。

## 四、平台约束备忘
1. **无循环节点** → 测试轮询沿用 type=6 代码节点内嵌 asyncio 轮询（360次/30min）
2. **串行+状态** → 幂等/续跑回放靠 `save_node_result`+`query_node_result` 自查链路（wf_sub_02~05 已具备）
3. **req_id 统一贯穿** → 环节1 生成后 2~9 全程沿用，各子流开始节点入参传值
4. **知识库/模板注册表** → 作为代码节点与提示词的查询资产随包迁移

## 五、验收口径
- 9 环节（需求提报→需求分析→智能配置→规格稽核→资费校准→自动测试→受理验证→上线审批→监控运维）全部有对应子工作流且标题头可辨
- 端到端：需求提报→审批门禁→模板轨方案→四环节→上线审批→自动上线→监控运维方案+看板
- 融合品/单商品双通道均可跑通
- skill 废弃后能力零回退（对照第二节差距表逐项回归）
- **工作流全量重建校验**：改生成器后依次执行 `python gen_workflows_v2.py` → `python gen_import_requests.py` → `python _validate_flows.py`（仓库根），12 个工作流全部 OK
- **执行工具回归**：`wf_runner.py --offline` 批量跑 12 个子工作流，全部 rc=0 / 错误 0
