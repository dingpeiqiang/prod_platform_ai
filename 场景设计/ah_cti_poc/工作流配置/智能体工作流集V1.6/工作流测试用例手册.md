# 智能体工作流集 V1.6 — 测试用例手册

> 测试对象：`场景设计/ah_cti_poc/工作流配置/智能体工作流集V1.6/` 下 12 个子工作流 `wf_sub_00`~`wf_sub_11`。
> 测试侧重：**节点链路 + 入参出参**（三层引用一致性、出参声明、分支走向）。
> 交付形式：本文档（Markdown）。可配合 `_validate_flows.py` 做 JSON 结构性回归校验。
> 节点类型：`0`开始 / `1`LLM / `2`分支 / `3`插件 / `6`代码 / `9`结束。
> 约定：节点 id 简写为尾 12 位，如 `...000000000001`；`<- B.O` 表示引用节点 `B` 的出参 `O`。

---

## 0. 通用静态校验（全部工作流共用）

| 编号 | 规则 | 通过条件 |
|---|---|---|
| G-1 | 节点类型白名单 | 仅允许 `{0,1,2,3,6,9}`，无其他类型 |
| G-2 | 开始节点存在且唯一 | 有且仅有 1 个 type=0，置于链路起点 |
| G-3 | 结束节点存在 | 至少有 1 个 type=9 |
| G-4 | 边可达 | 图中每个非开始节点均有入边，无非开始孤立节点 |
| G-5 | ref 入参三层一致 | 每个 type=ref 入参：`blockID`、`nameValue[0]`、`currValue` 前缀均为上游 `id`；`nameValue[1]`/`currValue` 整体为 `{上游id},{出参名}` |
| G-6 | ref 出参可解析 | 每个 type=ref 入参的 `relName` 均能在上游节点声明中找到同名出参 |
| G-7 | 代码节点出参格式 | 每个 type=6 节点的 `outputs` 均含 `relName` 且 `relName = "{自身id},{出参名}"`、`name` 与之同名 |
| G-8 | chat_id 可选透传 | 开始节点含 `chat_id` 选填入参（`required=false`），不强制下游透传 |
| G-9 | 结束节点页面地址 | 每个 type=9 节点的 `outputs.content` 末尾含 ```` ```xsbot-panel ```` 代码块；块内 JSON 的 `panels` 含两个外链面板（配置工作台 `view=workbench` + 环节业务页 `view=stage`），且块内 `{占位符}` 均为该节点已声明入参 |

> 已用 `_validate_flows.py` 对 12 个 JSON 实测：G-1~G-8 全部通过（见各节 `静态校验: 通过`）。
> G-9 由结束节点模板统一生成（`xsbot_panel_block`），14 个结束节点全部通过（块内 JSON 可解析、占位符与入参逐字一致）。

---

## 1. wf_sub_00 需求提报

### 1.1 链路
`开始(1) → LLM需求字段抽取(2) → 代码需求提报单渲染(3) → 分支待补充判定(4) ──0── 插保存需求工单(5) → 插需求工单审批(6) → 结束提报成功(7)`
`                                                                      └──-1── 结束有待补充(8)`

### 1.2 关键节点断言

| 节点 | 类型 | 入参引用 | 出参声明 | 断言 |
|---|---|---|---|---|
| 2 需求字段抽取 | 1 LLM | `requirement_text <- 1.requirement_text` | `elements_json` | 入参三层一致；prompt 引用开始节点需求文本 |
| 3 需求提报单渲染 | 6 代码 | `elements_json <- 2.elements_json`；`reporter <- 1.reporter` | `report_text,pending_fields,pending_count,req_id,elements` | 出参 relName=`...003,{出参名}` 格式；仅 2 个 ref 入参（req_id/need_summary 已删） |
| 4 待补充判定 | 2 分支 | 条件左值 `...003.pending_count` | — | 条件=**pending_count 不为空**(op10) |
| 5 保存需求工单 | 3 插件 | `req_id <- 3.req_id`；`result_json <- 3.elements`；node_name/status 常量 | `code,msg,record_id` | ref 指向代码节点出参 |
| 6 需求工单审批 | 3 插件 | `offer_id <- 3.req_id`；`report_url <- 3.report_text`；`req_id <- 3.req_id`；approval_type=requirement 常量 | `approval_id,status` | 双轨：approval_type=requirement |
| 7 结束提报成功 | 9 | `req_id,report_text <- 3`；`approval_id <- 6` | — | 无待补充分支终点 |
| 8 结束有待补充 | 9 | `req_id,report_text,pending_fields <- 3` | — | 有待补充分支终点 |

### 1.3 分支走向用例

| 用例 | 前置 | 预期走向 | 结束节点 |
|---|---|---|---|
| WF00-01 | pending_count 为空（无待补充字段） | 4→5→6→7 | 结束(提报成功) |
| WF00-02 | pending_count 长度>0（存在待补充字段） | 4→8 | 结束(有待补充) |

### 1.4 关键边界
- 需求文本空/全部字段未提及 → LLM 输出全空串 elements_json → 代码节点判定 price/resources 为空 → pending_count>0 → 走 WF00-02。
- code 节点未传 req_id 入参时自动生成 `PLAN+时间戳+3位随机`。

---

## 2. wf_sub_01 需求分析
> 模板轨，串行 11 节点（开始+9 功能+结束），无分支。

### 2.1 链路
`开始(101) → 读取需求工单(102) → 提取需求字段原文(103) → LLM产品识别(104) → 相似产品检索(105) → 获取配置模板(106) → LLM要素提取(107) → 要素校验与方案合并(108) → 方案表格渲染(109) → 保存执行方案(110) → 结束(111)`

### 2.2 关键节点断言

| 节点 | 类型 | 入参引用 | 出参 | 断言 |
|---|---|---|---|---|
| 102 读取需求工单 | 3 | `req_id <- 101.req_id`；node_name=requirement_report 常量 | `list` | 按 req_id+requirement_report 读取 |
| 103 提取需求字段原文 | 6 | `query_list <- 102.list` | `record_json` | 出参 relName 格式 |
| 104 产品识别 | 1 | `elements_record <- 103.record_json` | `template_id, need_summary` | LLM 选模板（六选一） |
| 105 相似产品检索 | 3 | `businessDesc <- 104.need_summary`；`templateId <- 104.template_id` | `similarOffer` | 返回 offerModel 存量报文 |
| 106 获取配置模板 | 6 | `template_id <- 104.template_id` | `template_id, schema_json, missing` | 出参含 schema_json |
| 107 要素提取 | 1 | `elements_record <- 103.record_json`；`schema_json <- 106` | `elements_json` | 按模板 x-label 提取 |
| 108 要素校验与方案合并 | 6 | `schema_json <- 106`；`elements_json <- 107`；`offer_json <- 105.similarOffer`；threshold 常量 | `payload, meta, pending_required, template, quality_gate, ve_result, ve_stats` | 六项校验+E31 门禁与递归合并合一（价格不照搬） |
| 109 渲染表格 | 6 | `schema_json <- 106`；`payload/meta <- 108`；`similar_offer <- 105` | `table_text, pending_count` | 四列业务分节表 |
| 110 保存执行方案 | 3 | `req_id <- 101`；`result_json <- 108.payload` | `record_id` | node_name=requirement |
| 111 结束 | 9 | `table_text <- 109`；`pending_required/quality_gate/ve_stats <- 108`；`template_id <- 106`；`req_id <- 101` | — | 结束语含方案表+待补充+质量门禁 |

### 2.3 用例

| 用例 | 前置 | 预期 |
|---|---|---|
| WF01-01 | 需求工单存在、模板命中、要素命中率≥阈值 | 全链路至 111，quality_gate=PASS，结束语含 table_text |
| WF01-02 | 106 缺模板（missing 非空） | schema_json 兜底，流程仍走通（模板目录缺失降级） |
| WF01-03 | 相似产品未命中（similarOffer 空） | 合并仅用需求要素，offer_json 兜底为空对象 |
| WF01-04 | 要素命中率<阈值（0.30） | quality_gate=FAIL，仍合并渲染（校验不阻断），结束语标注门禁 FAIL |
| WF01-05 | 需求缺少价格、相似产品有价格 | 价格字段不从相似品照搬，留空进 pending_required |
| WF01-06 | 要素含 schema 非法路径 | 108 剔除非法路径（ve_stats.removed_total>0），payload 不含该键 |

### 2.4 精简说明（V1.6 变更）
- 原 13 节点精简为 11 节点（8 个功能环节）。合并/删除项：
  - `要素提取质量校验`(原108) 与 `方案报文合并`(原109) 合并为 `要素校验与方案合并`(108)：单节点内先做 validate_elements 六项校验 + E31 可提取命中率门禁，再做 merge_nested 递归合并，输出 `quality_gate/ve_result/ve_stats` 与 `payload/meta/pending_required`。
  - 删除 `本体校验闸 validate_nested`(原110)：该环节依赖后端 HTTP 端点，不可达时本就回退占位，且结论不参与任何下游决策。
  - 删除无下游消费的出参：`提取需求字段原文` 的 `offer_id`、`获取配置模板` 的 `template_name_cn/template_product_type/schema_file`。
- 下游契约不变：`save_node_result` 仍以 `node_name=requirement` + `req_id` 存储实例化逻辑模型报文，wf_sub_02~06 读取方式无需改动。


---

## 3. wf_sub_02 智能配置

### 3.1 链路
`开始(101) → 读执行方案(102) → 取方案原文(106) → 生成商品编码(111) → 执行配置(103) → 融合成员处理(107) → 节点结果存储(105) → 结束(104)`

### 3.2 关键节点断言

| 节点 | 类型 | 入参引用 | 出参 | 断言 |
|---|---|---|---|---|
| 102 读执行方案 | 3 | `req_id <- 101`；node_name=execution_plan | `list` | 自读方案 |
| 106 取方案原文 | 6 | `query_list <- 102.list` | `record_json, offer_id` | 出参 relName |
| 111 生成商品编码 | 6 | `existing_offer_id <- 106.offer_id`；`plan_json <- 106.record_json` | `offer_id` | 新增品生成9位`900`编码；存量品透传既有9位编码 |
| 103 执行配置 | 3 | `req_id <- 101`；`plan_json <- 106.record_json`；`offer_id <- 111.offer_id`；confirmed/operator 常量 | `offer_id,save_result,status,product_config` | 配置落地 |
| 107 融合成员处理 | 6 | `group_json <- 103.product_config`；`status <- 103.status` | `fusion_echo, fusion_status` | 融合品展开 |
| 105 节点结果存储 | 3 | `req_id <- 101`；`result_json <- 103.product_config` | `record_id` | node_name=config |
| 104 结束 | 9 | `offer_id/save_result/status <- 103`；`fusion_echo <- 107` | — | 结束语含配置结果 |

### 3.3 用例

| 用例 | 前置 | 预期 |
|---|---|---|
| WF02-01 | 方案存在、配置成功(status=ok) | 全链路至 104 |
| WF02-02 | 非融合品 | 107 视 product_config.product_type 非融合直接透传/空 fusion_echo，仍走通 |
| WF02-03 | 新增产品（plan 无 offer_id/similarOfferId 溯源键） | 111 生成 9 位 `900` 编码（如 900117022），103 落地入参 offer_id 为生成的编码 |
| WF02-04 | 存量品重配（plan 含相似/既有 9 位编码） | 111 透传既有编码，103 落地 offer_id 与既有编码一致 |

---

## 4. wf_sub_03 规格稽核

### 4.1 链路
`开始(201) → 读环节结果(206) → 取稽核原始(207) → 实时稽核(202) → LLM稽核建议(203) → 节点结果存储(205) → 结束(204)`

### 4.2 关键节点断言

| 节点 | 类型 | 入参引用 | 出参 | 断言 |
|---|---|---|---|---|
| 206 读环节结果 | 3 | `req_id <- 201` | `list` | 读 config 结果 |
| 207 取稽核原始 | 6 | `query_list <- 206.list` | `record_json, offer_id` | 出参 relName |
| 202 实时稽核 | 3 | `offer_id <- 207`；`config_json <- 207.record_json` | `pass,error_list,audit_summary,resultCode` | 实时 |
| 203 稽核建议 | 1 | `error_list,audit_summary <- 202`；`record_json,offer_id <- 207` | `audit_suggest` | LLM 归因 |
| 205 节点结果存储 | 3 | `req_id <- 201`；`result_json <- 203.audit_suggest` | `record_id` | node_name=spec |
| 204 结束 | 9 | `pass,error_list <- 202`；`audit_suggest <- 203` | — | 结束语含稽核结论 |

### 4.3 用例

| 用例 | 前置 | 预期 |
|---|---|---|
| WF03-01 | 稽核通过 pass=true | 结束语含通过+建议 |
| WF03-02 | 稽核存在 error_list 非空 | 结束语含问题明细与整改建议 |

---

## 5. wf_sub_04 自动测试

### 5.1 链路（精简版 11 节点）
`开始(301) → 读配置环节结果(309) → 提取配置结果原文(310) → 发起测试(302) → 轮询测试进度(304) → 查询测试结果(305) → 固定用例映射(315) → LLM测试报告(306) → 环节结果存储(308) → 测试报告下载(316) → 结束(307)`

### 5.2 关键节点断言

| 节点 | 类型 | 关键入参 | 出参 | 断言 |
|---|---|---|---|---|
| 309 读配置环节结果 | 3 | `req_id <- 301`；`node_name=config` | `list` | 上游自查 |
| 310 提取配置结果原文 | 6 | `query_list <- 309.list` | `record_json, offer_id` | 出参 relName |
| 302 发起测试 | 3 | `offerId <- 310.offer_id` | `globalId` | 受理验证发起 |
| 304 轮询测试进度 | 6 | `globalId <- 302` | `done,failed,failIndex,fail_reason` | 进度轮询 |
| 305 查询测试结果 | 3 | `globalId <- 302` | `testScenes,offerName,orderId,...` | 结果 |
| 315 固定用例映射 | 6 | `test_result_json <- 305.testScenes`；`offer_id/plan_json <- 310` | `cases_json,dimension_summary,overall_conclusion,defect_list,p0_pass,e26,e26_note,scene_cover` | 确定性映射 |
| 306 测试报告 | 1 | `testScenes,globalId,req_id,offerId,cases_json,dimension_summary,overall_conclusion,defect_list,scene_cover,e26,...` 多源 | `test_report` | 正式版 9 章节报告 |
| 308 环节结果存储 | 3 | `req_id <- 301`；`node_name=test`；`result_json <- 306` | `record_id` | 衔接 wf_sub_06 门禁 |
| 316 测试报告下载 | 6 | `record_id <- 308`；`offer_id <- 310` | `download_url,note` | 下载 |
| 307 结束 | 9 | `test_report <- 306`；`overall_conclusion <- 315`；`download_url/dl_note <- 316`；`offerId <- 310`；`globalId <- 302` | — | AOL-D-6 报告 |

### 5.3 用例

| 用例 | 前置 | 预期 |
|---|---|---|
| WF04-01 | done=true 测试完成 | 全链路至 307，含 test_report+download_url |
| WF04-02 | failed=true 存在缺陷 | 315 映射 P0 缺陷，报告含 defect_list |
| WF04-03 | 316 下载端点不可达 | 回退为下载引导，note 提示"暂不可用，见上方报告正文" |

### 5.4 精简说明（16→11 节点）

原「查询测试场景(303)」「自检稽核结果(311)」「提取稽核原文(312)」「自检资费结果(313)」「提取资费原文(314)」五个环节的出参无任何下游消费，属链路冗余，已删除；315 的 `spec_record/fee_record` 入参同步移除，306 报告中稽核/资费引用改为基于 315 确定性映射结果（维度统计/缺陷清单）。下游契约不变：仍以 `node_name=test` + `req_id` 存储，供 wf_sub_06 四环节门禁读取。

---

## 6. wf_sub_05 资费校准

### 6.1 链路
`开始(401) → 读环节结果(406) → 取校准原始(407) → 资费校验(402) → LLM风险总结(403) → 节点结果存储(405) → 结束(404)`

### 6.2 关键节点断言

| 节点 | 类型 | 关键入参 | 出参 | 断言 |
|---|---|---|---|---|
| 407 取校准原始 | 6 | `query_list <- 406.list` | `record_json, offer_id` | 出参 relName |
| 402 资费校验 | 3 | `config_json <- 407.record_json` | `pass, risk_list` | check_billing_rule |
| 403 风险总结 | 1 | `risk_list <- 402` | `risk_summary` | LLM 归纳 |
| 405 节点结果存储 | 3 | `req_id <- 401`；`result_json <- 403.risk_summary` | `record_id` | node_name=fee |
| 404 结束 | 9 | `pass,risk_list <- 402`；`risk_summary <- 403` | — | 校准结论 |

### 6.3 用例

| 用例 | 前置 | 预期 |
|---|---|---|
| WF05-01 | risk_list 空、pass=true | 结束语通过 |
| WF05-02 | risk_list 非空 | 结束语含风险清单与summary |

---

## 7. wf_sub_06 上线审批

### 7.1 链路
`开始(601) → [自检五环节:602→603/604→605/606→607/608→609/610→611] → 合成结构摘要(612) → LLM审批建议(613) → 存储(614) → 提交审批(615) → 查询审批状态(616) → 分支状态判定(617) ──0── 结束审批中(620)`
`                                                                  └──-1── 监控运维方案(618) → 脚本下载(621) → 结束通过自动(619)`

### 7.2 关键节点断言

| 节点 | 类型 | 关键入参 | 出参 | 断言 |
|---|---|---|---|---|
| 602/604/606/608/610 自检五环节 | 3 | 各 `req_id <- 601` | `list` | 依次读 config/spec/fee/test/plan |
| 603/605/607/609/611 取原文 | 6 | 各 `query_list <- 上游.list` | `record_json[,offer_id]` | 出参 relName |
| 612 合成结构摘要 | 6 | `config_result <- 603`；`spec_result <- 605`；`fee_result <- 607`；`test_result <- 609`；`requirement_result <- 611` | `summary_json, offer_id` | 五轨合成 |
| 613 审批建议 | 1 | `summary_json,offer_id <- 612` | `approval_suggest` | LLM |
| 615 提交审批 | 3 | `offer_id <- 612`；`report_url <- 613.approval_suggest`；`req_id <- 601`；approval_type=launch | `approval_id,status,approval_type` | 双轨 launch |
| 616 查询审批状态 | 6 | `approval_id <- 615`；`offer_id <- 612` | `status,current_node,approver,opinion,...` | 状态 |
| 617 状态判定 | 2 | 条件左值 `...616.status` | — | 条件=**status 等于"通过"**(op1) |
| 618 监控运维方案 | 1 | `approval_id <- 615`；`offer_id <- 612`；`offer_id <- 612`；`chat_id <- 601` | `monitor_plan` | 通过后自动 |
| 621 脚本下载 | 6 | `offer_id <- 612`；`approval_id <- 615` | `backend_pending,download_url,note` | 下载 |
| 619 结束通过 | 9 | `monitor_plan <- 618`；`download_url <- 621`；`approval_id,status <- 615/616` | — | 审批通过终点 |
| 620 结束审批中 | 9 | `approval_id,status <- 615/616`；`current_node,approver,opinion <- 616` | — | 未通过终点 |

### 7.3 分支走向用例

| 用例 | 前置 | 预期走向 | 结束节点 |
|---|---|---|---|
| WF06-01 | status=="通过" | 617(-1)→618→621→619 | 结束(审批通过-自动监控) |
| WF06-02 | status≠"通过"（审批中/驳回） | 617(0)→620 | 结束(审批中-请查询) |

---

## 8. wf_sub_07 监控运维

### 8.1 链路
`开始(701) → 指标查询(702) → 分支异常判定(703) ──0── LLM运营摘要(710) → 结束正常(711)`
`                                                └──-1── LLM告警文案(704) → 异常告警(705) → 根因分析(706) → LLM根因报告(707) → 创建工单(708) → 结束异常(709)`

### 8.2 关键节点断言

| 节点 | 类型 | 关键入参 | 出参 | 断言 |
|---|---|---|---|---|
| 702 指标查询 | 3 | `offer_id,date_range <- 701` | `offer_name,order_count,error_count,fee_error_rate,alarm_list,...` | query_product_monitor |
| 703 异常判定 | 2 | 条件左值 `...702.error_count` | — | 条件=**error_count 不等于"0"**(op2) |
| 710 运营摘要 | 1 | `offer_name,order_count,error_count,fee_error_rate,alarm_list <- 702` | `ops_summary` | 正常分支 |
| 711 结束正常 | 9 | `offer_name/offer_id/date_range <- 701`；`ops_summary <- 710`；`chat_id <- 701` | — | 正常终点 |
| 704 告警文案 | 1 | `offer_name,order_count,error_count,... <- 702` | `alert_content` | 异常分支 |
| 705 异常告警 | 3 | `offer_id <- 701`；`content <- 704.alert_content` | `alert_id,status` | 发告警 |
| 706 根因分析 | 6 | `offer_id <- 701` | `reason_engine,anomalies,paths,evidence_triples,swrl_fired,...` | 根因引擎 |
| 707 根因报告 | 1 | `backend_pending,reason_engine,anomalies,paths,... <- 706` | `root_cause_report` | LLM 归因 |
| 708 创建工单 | 6 | `offer_id <- 701` | `backend_pending,work_order_id,note` | 建单 |
| 709 结束异常 | 9 | `offer_name<-702`；`alert_id<-705`；`root_cause_report<-707`；`work_order_id<-708`；`offer_id/date_range/chat_id<-701` | — | 异常终点 |

### 8.3 分支走向用例

| 用例 | 前置 | 预期走向 | 结束节点 |
|---|---|---|---|
| WF07-01 | error_count=="0"（无异常） | 703(0)→710→711 | 结束(正常-运营摘要) |
| WF07-02 | error_count≠"0"（有异常） | 703(-1)→704→705→706→707→708→709 | 结束(异常-已告警+工单) |

---

## 9. wf_sub_08 审批进度查询

### 9.1 链路
`开始(801) → 审批状态查询(802) → LLM状态摘要(803) → 结束(804)`

### 9.2 断言

| 节点 | 类型 | 关键入参 | 出参 | 断言 |
|---|---|---|---|---|
| 802 审批状态查询 | 3 | `approval_id,offer_id <- 801` | `approval_id,status,approval_type,current_node,approver,opinion,update_time,approval_matrix` | query_approval_status 双轨 |
| 803 状态摘要 | 1 | `approval_id,status,approval_type,current_node,approver,opinion,update_time,approval_matrix <- 802` | `approval_summary` | LLM 摘要 |
| 804 结束 | 9 | `approval_summary <- 803` | — | 查询结论 |

### 9.3 用例

| 用例 | 前置 | 预期 |
|---|---|---|
| WF08-01 | 审批存在、有状态 | 结束语含 approval_summary |
| WF08-02 | approval_matrix 含多级审批链 | 摘要含当前审核人与意见 |

---

## 10. wf_sub_09 存量产品查询

### 10.1 链路
`开始(901) → 存量查询(902,代码CODE_OP_QUERY_OFFER) → LLM查询回复(903) → 结束(904)`

### 10.2 断言

| 节点 | 类型 | 关键入参 | 出参 | 断言 |
|---|---|---|---|---|
| 902 存量查询 | 6 | `offer_id <- 901`；`name <- 901`；`keyword <- 901` | `matched,matched_count,outcome,k4_note` | 出参 relName |
| 903 查询回复 | 1 | `matched,matched_count,outcome,k4_note <- 902` | `query_reply` | LLM 润色 |
| 904 结束 | 9 | `query_reply <- 903` | — | 查询结果 |

### 10.3 用例

| 用例 | 前置 | 预期 |
|---|---|---|
| WF09-01 | 传 offer_id 精确命中 | matched=1，回复含产品信息 |
| WF09-02 | 传 keyword 模糊查询 | matched_count≥1，回复列候选 |
| WF09-03 | 未命中 | 回复提示未找到/给出建议 |

---

## 11. wf_sub_10 存量合规扫描

### 11.1 链路
`开始(1001) → 存量合规扫描(1002,代码CODE_OP_SHELF_COMPLIANCE) → LLM合规报告(1003) → 结束(1004)`

### 11.2 断言

| 节点 | 类型 | 关键入参 | 出参 | 断言 |
|---|---|---|---|---|
| 1002 合规扫描 | 6 | （无 ref 入参，全量扫描） | `backend_pending, note` | 出参 relName |
| 1003 合规报告 | 1 | `backend_pending,note <- 1002` | `compliance_report` | LLM 报告 |
| 1004 结束 | 9 | `compliance_report <- 1003` | — | 合规结论 |

### 11.3 用例

| 用例 | 前置 | 预期 |
|---|---|---|
| WF10-01 | 扫描完成 backend_pending=false | 结束语含 compliance_report |
| WF10-02 | 异步后台扫描 backend_pending=true | 结束语提示后台生成中 |

---

## 12. 回归说明

- 任何改动须经 `python _validate_flows.py` 跑通 G-1~G-9（当前 12 个 JSON 全部 OK）。
- 全部 JSON 由 `gen_workflows_v2.py` 源码生成，禁止手改 JSON（源码驱动红线）。
- 修改生成器后需依次重跑：
  1. `python gen_workflows_v2.py`（重生成 wf_sub_*.json）
  2. `python gen_import_requests.py`（重生成导入请求 payload）
  3. `python _validate_flows.py`（回归校验）

---

## 13. 变更记录

- v1.1（2026-09-19）：新增 G-9 结束节点页面地址校验；11 个子工作流全部结束节点末尾追加双面板 `xsbot-panel` 外链块（配置工作台 / 环节业务页）；原型页落库 `frontend/public/ops-web/config-workbench.html`。
- v1.0：首版，覆盖 wf_sub_00~10 节点链路、入参出参与分支用例。
